# ZheWanBiRangCao

`ZheWanBiRangCao` 是一个基于 `NXOpen C++` 的半自动折弯避让槽插件。

当前规划：

- 用户选择一条折弯根边
- 程序自动分析该边两端是否需要开避让槽
- 按槽宽、槽深、圆角等参数生成工艺槽
- 以 `NXOpen` 为主实现几何识别和建模

推荐实现顺序：

1. 选单条折弯根边
2. 自动定位两端开槽点
3. 先支持矩形槽/圆头槽
4. 再加标准参数表和批量处理

如果后续某个交互行为需要完全贴合 NX 手动命令，可补充录制 `journal` 作为参考。
