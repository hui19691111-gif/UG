# 折弯模拟首版验证记录

日期：2026-09-21；NX 2412，Windows x64，Visual Studio 2022 Release。

## 几何集成测试

测试源为 `tests/Integration.cpp`，通过真实 NXOpen/UFUN 接口运行。最近一轮六件回归目录：`G:\ZeWanMoNiTest_20260921_03`（DWG 刀库更新后）。

| 样件 | 已通过检查 |
| --- | --- |
| bend-mm.prt | 毫米内圆柱面识别、定位、穿透、接触、0.2 mm 间隙、重复操作与回滚 |
| bend-rotated.prt | 旋转后的同类圆柱折弯，方向与上列结果一致 |
| bend-inch.prt | 英寸圆柱折弯的毫米输入换算及上列结果 |
| sharp-mm.prt | 毫米内侧锐边识别、虚拟 R 定位、分离状态检查与回滚 |
| sharp-rotated.prt | 旋转锐边的方向、定位与回滚 |
| sharp-inch.prt | 英寸锐边的单位、定位与回滚 |

还验证了外圆柱面/外侧凸边/平面拒绝、锐边无效 R、负长度、自定义刀具格式、自交与重复点拒绝。重复切换三种刀具、方向和倾角后，源体积和特征数量保持不变，零件 `IsModified()` 为 false；错误目标导致计算失败后也无临时特征残留。

穿透/接触/间隙三类断言使用圆柱样件；锐边样件仅断言明确分离状态，没有把未断言的分类列为已测试。

## NX 原生对话框人工操作验证

使用 computer-use 技能操作正在运行的 NX，测试零件为自行生成的样件；未编辑用户原有 `model2.prt`。

- 命令多次打开成功，中文路径加载 DLL 与 DLX 成功。
- 圆柱/锐边模式反复切换，选择清空和内 R 控件启用状态正确。
- 反转方向开/关、自动正视开/关均重复操作；预览和视角随设置更新。
- 有效内边替换为外侧凸边时提示错误，再选回内边可恢复预览。
- 圆柱零件显示接触、最小距离 0.000 mm；锐边抬刀 2 mm 时显示无干涉、最小距离 1.707 mm。
- 切换直刀、偏置刀与自定义示例后，轮廓随截面变化。
- 打开用户刀具目录并生成示例，重新加载后出现自定义条目，选中并“应用”检查成功。
- 修复了 NX 在“应用”后重新显示对话框时清空选择的问题；修复后连续应用保留选择和结果，关闭自动正视仍能恢复进入前的方向。
- 在自动正视开启、使用自定义刀具且已检查的状态下取消：对话框退出，临时轮廓清除，视角方向恢复，特征树仍为原始单个拉伸特征。

首次部署时未重启 NX 验证新菜单，以免打断已有零件会话。刀具列表更新部署后，已在用户当前 NX 会话中点击智辉功能区的“折弯模拟”命令，成功打开新列表和截面大图。

## 构建与部署

Release 构建成功；运行期仅部署 DLL、DLX、BMP 和 `.ztool` 示例，不部署集成测试 EXE 或调试符号。部署由 `tools/deploy.py` 执行，校验授权入口、受保护 Release 配置、优化、ASLR、DEP 和重算 PE checksum；备份、更新两个清单并逐文件比对 SHA-256。

首次部署于 2026-09-21 16:28:32 完成。备份目录：`D:\UG智辉钣金插件\backup\before-ZeWanMoNi_20260921_162832`。该次 DLL 大小 155648 字节，PE checksum 为 167120，SHA-256 为 `9392DE73563456FDBE67846F873669DE18B65A87DB42335232F1E8794C7B02AA`。

全部资源、备份目录和每个文件的哈希详见 `build-codex/deployment-verification.json`。

## 刀具列表更新验证（2026-09-21 16:54）

将刀具下拉框替换为名称和截面缩略图列表，增加选中截面大图及宽高。图片直接来自刀具多边形，不依赖第三方图片或预装刀库。验证环境仍为 NX 2412，测试件为 `sharp-mm.prt`。

- 列表显示 3 把内置演示刀具和 1 把用户目录中的自定义示例；逐项选择后图片、宽高与三维轮廓对应。
- 大图尺寸显示验证：直刀 16×60 mm、窄刀 12×60 mm、偏置刀 30×80 mm、自定义示例 10×50 mm。
- 发现并修复 Apply 会再次触发 Initialize 的生命周期问题：在预显示阶段重置就绪状态，清除旧节点句柄，仅在 Shown 中重建列表。
- 修复后自定义刀具和偏置刀分别执行 Apply 成功，列表、选中项、大图、折弯选择与检查结果均保留；未再出现树构造异常或无效参数异常。
- 重新加载自定义刀具后保留所选自定义项，大图仍可见，旧检查结果正确失效。
- 圆柱/锐边模式往返、方向反转开/关、自动正视开/关、无效外边替换为有效内边均正常；Apply 后取消正视恢复进入前视角。
- 取消后退出对话框并清除临时轮廓；特征树仍仅包含原拉伸。未修改用户 `model2.prt`。

此轮只修改界面与截面图片生成；几何算法未变，未重复六件几何集成测试。当前 DLL 已在 16:54:25 完成保护检查、备份与部署，大小 166912 字节，PE checksum 176497，SHA-256 为 `E1C1E41B0D33A9C89998FD49C33BBB02A64B1C0EAF99E38F51F7DD718964F4A7`。本轮备份目录：`D:\UG智辉钣金插件\backup\before-ZeWanMoNi_20260921_165425`。最新逐文件记录见上述 JSON。

## DWG 刀库更新验证（2026-09-21 17:16）

原图 `F:\BaiduNetdiskDownload\资料\折弯模拟.dwg` 的 SHA-256 为 `d18814035736c8e55c79ed6008deffcdc10ee3c0b336439b20b094c998027dfb`，读取和安装后再次核对一致。通过 AutoCAD ObjectDBX 读取副本；图纸变量确认毫米单位，提取 10 个审核后的上刀截面，细节见 `tool-library/dwg-20260921/catalog.json`。RZ8895 重复圆弧和未修剪刀尖线的处理有明确坐标记录，不自动补断线。

- 最终 DWG 几何测试目录 `G:\ZeWanMoNiTest_Dwg_20260921_05`。10 刀 × 2 单位/姿态 × 2 正反方向，共 40 个实体体积比对，以及各自抬高和近工件姿态检查，全部通过。抬高状态断言 Clear，近工件状态仅断言检测成功，并未逐刀认定生产折弯可行。
- 从多边形面积×40 mm 长度计算体积，NX 质量属性体积相对误差小于 1e-7。每次检查后零件体积、特征数、建模公差、IsModified 状态保持不变；退化截面导致失败时也验证回滚。
- RZ30104 在旋转英寸件上暴露了旧 UF 拉伸的 `Invalid string objects (1070007)`。改为显式 Section 与 ExtrudeBuilder，截面/拉伸公差为 0.0001 mm，经单位换算，未更改用户零件的公差设置。最终测试覆盖该失败场景。
- 更换实体创建接口后，完整重跑原有 6 个 L 型圆角/锐边、旋转/英寸样件，全部通过；穿透、接触、间隙和清理断言维持有效。日志为 `build-codex/dwg-library-test.log` 与 `build-codex/geometry-regression-test.log`。
- 在当前 NX 会话从功能区打开已部署命令，新增 DWG 名称及缩略图正常显示；核对 000 的 65.0×149.9 mm、004 的 40.0×95.0 mm、RW88120 的 65.1×149.9 mm 大图。使用 RW88120 定位到测试锐边并 Apply，返回当前姿态无干涉、最小距离 0.940 mm（现有抬刀、倾角参数），选中刀具和折弯输入保留。重新加载后刀具不变、旧结果失效。取消后临时轮廓清除，特征树仍仅原拉伸。
- 发布 DLL 170496 字节，PE checksum 207833，SHA-256 为 `44271E72717171F006EE432F8CD3F335D2B6EB12E5C00E6261347648CA416468`。授权入口、受保护优化 Release、ASLR、DEP 均通过检查；新增 NX 库依赖来自已安装 NX 2412。
- 运行期备份 `D:\UG智辉钣金插件\backup\before-ZeWanMoNi_20260921_171615`；用户刀库安装记录 `D:\UG智辉钣金插件\backup\before-ZeWanMoNi-user-tools_20260921_171616`。安装新增 10 个文件，没有替换已有用户刀具；列表总计 14 项（3 内置+10 DWG+1 自定义示例）。
- Release、插件安装目录、用户目录与目录 JSON 中的刀具哈希一致。两个清单的非本功能条目和保护元数据保持不变；未部署测试 EXE 或调试符号。

## 精简参数与局部干涉标红（2026-09-21 18:16）

- 删除全部数值输入，包括刀具长度；刀图提供截面，沿折弯轴长度自动匹配所选折弯。实际圆柱 R 读取模型，真正内侧锐边按 R=0 定位。选择类型自动识别，自动正视和自动检查，只保留方向反转。
- 黄色显示刀具轮廓。穿透时使用保留源体的 NX BooleanBuilder 求交，提取所有交集实体边界为红色粗线。只复制坐标用于显示，临时刀具、求交结果和特征全部通过不可见撤销标记回滚。接触不生成红线，曲线显示弓高误差上限 0.005 mm。
- 六件回归目录 `G:\ZeWanMoNiTest_Red_20260921_03`，日志 `build-codex/red-boundary-test.log`。毫米、旋转毫米、英寸圆柱样件均通过实际重叠边界点同时属于工件和刀具的断言；分离的两个干涉区域均被提取；接触无红线。锐边 R=0 原位和负 R 拒绝通过。所有检查回滚后体积、特征数和修改状态不变。
- DWG 回归目录 `G:\ZeWanMoNiTest_RedLibrary_20260921_01`，日志 `build-codex/red-library-test.log`。10 把刀的毫米/旋转英寸、正反方向、实体体积、近工件/分离状态及失败回滚全部通过。
- 在用户当前 `model2.prt` 上实际操作新对话框：识别内 R 0.20 mm，自动刀长 65.16 mm；90° 示例刀显示接触、距离 0.000 mm，无红线；DWG 004 平底刀显示穿透，局部重叠边界显示红色。
- 圆弧旁相切边已从锐边候选中排除。外圆/外侧凸边错误输入后重新选内圆可恢复；换刀自动刷新，反转开关两轮无异常。Apply 保留刀具、选择与结果；重载刀库保留选择并自动重检；Cancel 清除临时显示并恢复视角方向。用户特征树仍为原来的三个钣金特征，未保存用户文件。
- Release 构建、受保护优化配置、授权入口、ASLR、DEP 和 PE checksum 检查通过。安装 DLL 175104 字节，checksum 223445，SHA-256 `F15EAE09EE64AE94838978FD840DB3CFB8E66EBE003ECE28694B7135F9F4110B`。运行期资源与 Release 输出逐文件一致，两个清单已同步。
- 备份目录 `D:\UG智辉钣金插件\backup\before-ZeWanMoNi_20260921_181614`。以上旧版操作记录保留为历史，当前使用方式以本节和使用说明为准。
- 安装后从智辉功能区重新打开“折弯模拟”，确认精简界面正常加载；重新选内圆弧得到同样的 R 和自动刀长，并确认接触/换刀后的干涉显示。当前 NX 保留 DWG 004 的局部干涉预览，便于用户直接查看。

## DWG 预览导入与智辉刀图目录（2026-09-23）

- 在 NX 2412 的 Release ABI 测试构建中，导入 `折弯模拟.dwg` 得到 10 个候选轮廓。切换 RW88120 与 RW8867 后，列表缩略图、大图及宽高随所选轮廓更新；保存 RW8867 后验证生成的 `.ztool` 可重新读取，存档 DWG 的 SHA-256 与原图一致。
- 用户刀库统一为 `D:\UG智辉钣金插件\刀图`，原始 DWG 放在 `刀图\原图`。安装了 10 个已审核截面并迁移旧用户示例；安装目录原图 SHA-256 为 `D18814035736C8E55C79ED6008DEFFCDC10EE3C0B336439B20B094C998027DFB`，与源码副本一致。
- `DwgImportIntegration` 对原图识别 10 个候选轮廓并完成刀具格式校验；`BendSimulationIntegration` 对 10 个已审核刀具的毫米与旋转英寸模型执行几何、碰撞及回滚回归，均通过。对话框方向反转往返、选择替换、Apply 报错后保持打开、Cancel 清理已检查。未保存用户的 `model2.prt`。
- 最终受保护 Release DLL 通过 ASLR、DEP、授权入口和非零 PE checksum 检查，安装文件与构建输出哈希一致；部署备份为 `D:\UG智辉钣金插件\backup\before-ZeWanMoNi_20260923_111919`，详细哈希见 `build-codex/deployment-verification.json`。
- 当前 NX 进程在本轮调试时已预加载开发目录的同名授权 DLL。安装版在此会话中被该开发模块指向的缺失清单拦截；这次安装版界面复测需在重新启动 NX 后进行。程序现在会显示授权失败原因，不再静默退出。开发测试构建已完成上述界面操作。

## 尚未验证

刀具实物与原图的一致性、除上述当前零件以外的生产零件形态、其他 NX 版本、多折箱体、孔槽附近折弯、复杂拆分面、完整折弯运动过程、上下模与机床联动均不属于本次通过范围。本次验证的是用户图纸转换的刀具几何和单刀静态检测。
