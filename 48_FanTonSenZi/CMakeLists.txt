cmake_minimum_required(VERSION 3.20)
project(FanTonSenZi LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
if(NOT NX_ROOT)
  set(NX_ROOT "$ENV{UGII_BASE_DIR}")
endif()
if(NOT EXISTS "${NX_ROOT}/UGOPEN/uf.h")
  message(FATAL_ERROR "Set NX_ROOT to the NX installation directory")
endif()
function(configure_nx target)
  target_include_directories(${target} PRIVATE "${NX_ROOT}/UGOPEN" "${NX_ROOT}/UGOPEN/NXOpen")
  target_compile_definitions(${target} PRIVATE UNICODE _UNICODE NOMINMAX _CRT_SECURE_NO_WARNINGS $<$<CONFIG:Release>:ZH_PROTECTED_BUILD=1>)
  target_compile_options(${target} PRIVATE /W4 /EHsc /utf-8 $<$<CONFIG:Release>:/O2>)
  target_link_directories(${target} PRIVATE "${NX_ROOT}/UGOPEN")
  target_link_libraries(${target} PRIVATE libufun.lib libugopenint.lib libnxopencpp.lib libnxopencpp_gateway.lib libnxopencpp_features.lib libnxopencpp_geometricutilities.lib libnxopenuicpp.lib user32.lib)
  target_link_options(${target} PRIVATE $<$<CONFIG:Release>:/RELEASE> /DYNAMICBASE /NXCOMPAT)
endfunction()
add_library(FanTonSenZiCore SHARED FanTonSenZiCore.cpp)
configure_nx(FanTonSenZiCore)
add_library(FanTonSenZi SHARED TubeCustomFeature.cpp FanTonSenZi.cpp FanTonSenZiMain.cpp TubePlan.cpp TubeInspect.cpp TubeRoundInspect.cpp TubeCreate.cpp)
configure_nx(FanTonSenZi)
add_dependencies(FanTonSenZi FanTonSenZiCore)
foreach(resource FanTonSenZi.dlx FanTonSenZi.bmp CustomFeatureConfiguration.xml)
  add_custom_command(TARGET FanTonSenZi POST_BUILD COMMAND ${CMAKE_COMMAND} -E copy_if_different "${CMAKE_CURRENT_SOURCE_DIR}/${resource}" "$<TARGET_FILE_DIR:FanTonSenZi>/${resource}")
endforeach()
option(ZH_TUBE_TESTS "Build separate NX verification programs" OFF)
if(ZH_TUBE_TESTS)
  add_executable(TubeCustomTests tests/CustomFeature.cpp TubeCustomFeature.cpp TubePlan.cpp TubeInspect.cpp TubeRoundInspect.cpp TubeCreate.cpp)
  configure_nx(TubeCustomTests)
  add_executable(TubeUserModel tests/UserModel.cpp TubePlan.cpp TubeInspect.cpp TubeRoundInspect.cpp TubeCreate.cpp)
  configure_nx(TubeUserModel)
  add_executable(TubeInspectPart tests/InspectPart.cpp TubePlan.cpp TubeInspect.cpp TubeRoundInspect.cpp)
  configure_nx(TubeInspectPart)
  add_library(TubeUITest SHARED tests/LaunchUI.cpp)
  configure_nx(TubeUITest)
  add_executable(TubePlanTests tests/PlanTests.cpp TubePlan.cpp)
  target_compile_options(TubePlanTests PRIVATE /utf-8)
  add_executable(TubeIntegration tests/Integration.cpp TubePlan.cpp TubeInspect.cpp TubeRoundInspect.cpp TubeCreate.cpp)
  configure_nx(TubeIntegration)
  add_executable(TubeRoundTests tests/RoundTests.cpp TubePlan.cpp TubeInspect.cpp TubeRoundInspect.cpp TubeCreate.cpp)
  configure_nx(TubeRoundTests)
  add_executable(TubeSourceSlots tests/SourceSlots.cpp TubePlan.cpp TubeInspect.cpp TubeRoundInspect.cpp TubeCreate.cpp)
  configure_nx(TubeSourceSlots)
  add_executable(TubeMachineBend tests/MachineBend.cpp TubePlan.cpp TubeInspect.cpp TubeRoundInspect.cpp TubeCreate.cpp)
  configure_nx(TubeMachineBend)
  add_executable(TubeObliqueTests tests/ObliqueTests.cpp TubePlan.cpp TubeInspect.cpp TubeRoundInspect.cpp TubeCreate.cpp)
  configure_nx(TubeObliqueTests)
endif()
