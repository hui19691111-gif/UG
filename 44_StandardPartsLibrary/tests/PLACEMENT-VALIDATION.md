# 插入点、确认按钮与预览修复验证记录（2026-09-06）

## 最新部署：主界面下拉框直接选择参数组（2026-09-08 09:59）

- 按用户截图，将“选择参数”右侧下拉框用于直接选择已保存的 A组、B组等命名参数组。切换后同步已定义参数摘要、实际规格和插入预览；每个下拉项始终映射实际 PRT 索引，避免参数组序号被当成规格序号。没有参数组的零件族保留原规格选择方式。
- 当前尺寸自动匹配已有组；不匹配任何组时显示“当前尺寸（自定义）”，保留当前实际规格。组保存、删除后刷新主界面；参数编辑窗口同步主界面选中的组，继续用于修改尺寸和管理组。
- 已有尺寸组合复用现有规格。首次生成组合使用原有退出 BlockDialog 后保存并重新打开的流程，同时传递组名、插入点/方位和放置选择；切换回调捕获并记录 NX、标准及未知异常，失败时恢复当前实际规格。
- 五个受影响目标 Release 构建成功，日志 build-codex/build-main-groups.log。参数集成 10 组、放置及预览集成 13 组、两份 DLX 配置校验通过；日志 test-main-groups-parameters.log、test-main-groups-placement.log。新增验证包括主下拉组名、真实规格索引、尺寸摘要、刷新保持、新组生成、删除后自定义尺寸回退及无组零件族兼容。
- NX2412 PID 31316 从正式工具栏加载部署版本，使用独立 main-groups-final 库验证：主下拉显示 A组/B组/D组，A组 100×50×30 与 B组 80×40×20 连续切换两轮均立即更新；面中心选定后首次选择 D组生成 70×35×25，新窗口恢复 D组、选面及定位，模型预览同步更新。
- 在独立验证装配中点击“应用”实际插入 D组方体，主界面保持可用；再打开“选择参数”，D组及 70/35/25 输入正确，参数窗口取消、主窗口取消及重新打开均正常。验证装配 build-codex/参数组界面验证_20260908.prt 已保存并关闭；库根目录经界面恢复 D:\UG智辉标准件库，并核对注册表。原有两个用户工作图档保留，没有在用户图档中插入、保存或关闭。
- 构建完成后顺序备份和部署，备份 D:\UG智辉钣金插件\backup\StandardPartsLibrary_main_groups_20260908_095919，包含旧四个运行时文件、活动清单及参考清单、说明和库清单。四个部署文件与 Release SHA-256/字节数一致，活动文件清单与包命令哈希已更新，授权、防篡改和其他保护元数据保留，没有部署调试附属文件。
- DLL 515584 字节，SHA-256 51E103D4DC6B0B7D717B07FC9286D83A30D41E35A502E3C44A6DEC29CC875ECD；PE checksum 0x89A8E（重新计算一致），DLL characteristics 0x160。Release MaxSpeed、ZH_PROTECTED_BUILD=1、ASLR/DEP 保留。部署及原生验证结果记录 build-codex/deploy-main-groups.json。

## 历史部署：X/Y 数量、右键预览图与有参参数组（2026-09-08）

- X/Y 轴排列改为分别读取 X数/Y数及对应间距，按数量围绕插入点等距展开；预览和实际插入共用位置计算，保留局部插入方位。单个、对角、四角、居中、二维阵列的数量规则保持原有语义。
- 标准件列表右键增加更换预览图片、抓图作为预览；本地有参增加参数组/选择尺寸和修改参数设定。图片使用 WIC 解码并转为同名 PNG，原图备份，原子替换后直接重读，绕过系统缩略图缓存；图片操作不修改 PRT。
- 同一零件族支持 A/B 等命名尺寸方案，按表达式绑定整组输入值。切换、同名更新、新名称保存、删除、数值与列表验证、模式变化检查及失败回滚均已实现；固定与公式参数不作为自由输入保存。插入窗口仍只展示已定义参数的中文名称、值和输入类型。
- 五个受影响目标 Release 构建成功，见 build-codex/build-groups-final.log；外部 NX 参数集成 9 组、放置与预览集成 13 组、两份 DLX 配置检查通过。日志 test-groups-parameters.log、test-groups-placement.log。实际原生模型验证 X 轴 5 个组件、Y 轴 4 个实体及准确坐标；方体 A 组 100×50×30 复用已有规格，B 组生成准确的 80×40×20 实体；参数组保存不改变模板和撤销标记，损坏图片及文件写入失败不会破坏原文件。
- 在 NX2412 PID 31316 使用正式安装 DLL 和独立 groups-test 测试库进行界面验证：打开参数窗口仅显示长/宽/高；B→A→B 切换正确带入 80/40/20、100/50/30、80/40/20；同名保存正常，选用 B 组返回对应规格。右键修改参数设定正常打开现有三行并保存返回；右键更换 PNG 后预览立即出现，关闭并重新打开主命令正常。
- 同一独立验证装配中，选面中心后 X 数量从 2 改为 5，屏幕预览为 5 个，应用后装配导航器显示 static × 5；继续切换 Y 轴、数量 4、重新选择另一面，四件预览及方位正确，第二次应用后显示 static × 9，主窗口保持可用，取消正常退出。保存并关闭的验证模型为 build-codex/参数组界面验证_20260908.prt。原有两个用户工作部件保留；库根目录已通过界面恢复为 D:\UG智辉标准件库，并读取注册表确认。新增右键抓图入口已接入原有无高亮抓图流程，本轮未重复屏幕抓图交互。
- 构建完成后顺序备份并部署，备份 D:\UG智辉钣金插件\backup\StandardPartsLibrary_groups_20260908_092804，包含旧四个运行时文件、两份活动清单、源码参考清单及说明/库清单。四个部署文件与 Release 哈希、长度一致；更新运行时 SHA-256/字节数、包命令 SHA-256 与说明文件库清单。授权、防篡改及其他保护元数据保留，无调试文件部署。
- DLL 504832 字节，SHA-256 7A68F0225C0A2A216BB28C258EB418BB58C87D168C68AFB54323736FDCAC5EF0，PE checksum 0x86A5D（重新计算一致），DLL characteristics 0x160；MaxSpeed、ZH_PROTECTED_BUILD=1、ASLR/DEP 保留。记录 build-codex/deploy-groups.json。

## 历史部署：标准件库图标重新设计（2026-09-08 00:08）

- 用蓝色零件托盘、内六角螺丝、螺母及橙色参数滑块表达标准件库与用户定义参数。内置 imagegen 原图及生成提示词保存在 assets/icon，导出透明 256/32 像素 PNG 和正式 32×32 BMP；BMP 沿用旧资源的 32 位 RGB 格式及 RGB(209,209,209) 背景键，检查大小图后主体可辨认。
- 本轮仅替换图标资源，运行时源码、两个 DLX 和用户模型/库数据未修改。StandardPartsLibrary 目标强制重新 Release 构建成功，日志 build-codex/build-icon-release.log；仅有既存 NX SDK C4275 警告。本轮未重复几何集成测试，未操作用户正在使用的 NX 制图窗口；运行中的 NX 可能缓存旧图标，重启后加载新版。
- 备份 D:\UG智辉钣金插件\backup\StandardPartsLibrary_icon_20260908_000715，含旧四个运行时文件、两份活动清单、源码参考清单及源码旧图标。构建完成后顺序部署四个 Release 文件，校验部署文件与 Release SHA-256/长度一致并更新活动清单，源码参考清单同步命令哈希。所有授权、防篡改及其他包元数据保持不变，无调试文件部署。
- DLL 473088 字节，SHA-256 AE4687460245A5BE21482AFD0BC512F0DE2FF53664B42A32FC3D3B166235AB25，PE checksum 0x7434D（重新计算一致），DLL characteristics 0x160；Release MaxSpeed、ZH_PROTECTED_BUILD=1、ASLR/DEP 保留。BMP 4150 字节，SHA-256 E85F38635FF611EDAF3A11DB24C9776D04182DE9286B11B316B24900EFE45EF4。记录 build-codex/deploy-icon.json。

## 历史部署：仅显示用户定义的调用参数（2026-09-07 22:12）

- 删除新建参数定义时自动填入前 12 个表达式的行为。模型表达式只作为候选项；未定义模板打开空表，已有参数定义按保存的行加载。插入“选择参数”仅显示定义中的名称、值与输入类型，表达式绑定保留在定义和管理界面。
- 按用户本次要求整理 model3：调用定义仅保留 p7/长、p8/宽、p1/高，清除此前自动加入的 9 个 Sheet_Metal 内部表达式行。规格表投影保留默认 100×50×30 及用户新建 50×10×40 两条规格；两份 PRT、原 PNG 和库索引字节不变，模型内部表达式未改。变更前定义、完整规格表和索引已备份。
- 全部 5 个目标 Release 构建成功，日志 build-codex/build-defined-parameters-only.log。既有参数集成测试 7 组及两份 DLX 校验全部通过（test-defined-only.log、test-defined-only-dialog.log）；本次未改动 Block Styler 生命周期或几何逻辑。
- 实际 NX2412 PID 25760 从正式工具栏加载部署 DLL：model3 主界面仅显示长宽高，“选择参数”只有三行、无内部表达式列；长度下拉值 20/50/80/100/150 正确，选用现有默认规格正常返回。用独立 BlockTemplate.prt 打开“选择 PRT 新建有参”，定义表 12 行均为空，取消不创建图档。全程未在用户工作图档执行插入、保存或关闭。
- 备份 D:\UG智辉钣金插件\backup\StandardPartsLibrary_defined_only_20260907_221248，含旧四个运行时文件、两份活动清单、源码参考清单及 model3/说明/库包清单备份。顺序部署四个 Release 文件，更新活动清单 SHA-256/字节数和命令哈希；授权及防篡改元数据保持不变。DLL 473088 字节，SHA-256 56428EE254FDE137B67FFA0241261CC0D875BBF621DF9F1553FC78CCB65F81C2，PE checksum 0x80D12，DLL characteristics 0x160，优化保护构建保留，无调试文件部署。记录 build-codex/deploy-defined-only.json。

## 历史部署：插入时选择用户定义参数（2026-09-07 21:14）

- 修复已定义的参数只能在图档管理修改、插入主界面只能查看摘要的问题。本地有参主界面增加“选择参数”，显示全部已定义的数值/下拉项，固定和公式项只读。选用时优先匹配当前及同族现有规格；缺少组合时自动命名、生成原生有参规格并选中，保留源模板，适用于任意已定义数值表达式的零件族。
- 生成新规格仍在 NX BlockDialog 退出、销毁后执行，避免保存部件清除活动撤销标记。生成前捕获工作部件、放置选择、插入点/方位、模式、阵列、图层及修剪选项；返回主界面后在已显示阶段恢复，程序选择变更受重入保护，失效对象不恢复。
- 全部 5 个目标 Release 构建成功（build-codex/build-insert-parameters.log）。参数测试 7 组通过（test-insert-parameters-final.log），含现有规格复用、不重复生成、撤销标记保持及非螺钉模型 LENGTH/WIDTH/HEIGHT 数值、列表、公式联动的 80×35×20 原生实体验证；放置/捕获等 11 组既有回归通过（test-insert-placement-final.log）。两个 DLX 校验通过（test-insert-dialog.log）。补充只读尺寸核验测试目标已重新 Release 构建。
- 实际 NX2412 PID 25760：四种放置方式各切换两轮，基准面连续换选侧面、顶面、端面。使用用户 model3 的独立副本，在“选择参数”中将 p7/长、p8/宽、p1/高分别选为 80、30、20；自动生成规格后，原有端面选择、插入点及非默认方位保留。再次选用相同参数复用已有规格。分别执行装配、多实体“应用”，实体可见且主界面继续可用；参数窗口取消、主界面取消均正常，无可见回调错误或等待卡死。
- 只读打开界面生成的 PRT，实体包围盒尺寸为 80×30×20，文件读取前后字节不变（test-insert-model3-dimensions.log）。测试装配已保存、关闭，两个测试规格及参数表归档至 build-codex/native-insert-parameters-20260907/created-family；仅移除两条测试索引，剩余 165 条与测试前基线完全一致，SHA-256 E77BB12ABF2FE0B52445301398D2E9D5D3CEA97427709409A1E53D87CA765844。用户原有 model3 PRT、schema 和参数表哈希保持不变；未保存或关闭用户 model1，结束后返回该工作图档。
- 备份 D:\UG智辉钣金插件\backup\StandardPartsLibrary_insert_parameters_20260907_211446，含原 4 个运行时文件、两份活动清单、源码参考清单，以及更新前的用户说明和库包清单。构建及界面回归通过后顺序替换正式目录。DLL 472576 字节，SHA-256 6C20BF04180A133E66919BCFC1A85043B3C68DBCA88FF6B3B4C29CB89BD9D0D3，PE checksum 0x82D8D，DLL characteristics 0x160。Release MaxSpeed、ZH_PROTECTED_BUILD=1、ASLR/DEP、三个导出入口及授权/防篡改元数据保留，未部署调试附属文件。记录 build-codex/deploy-insert-parameters.json。
- 安装后从正式工具栏启动，核对实际加载 D:\UG智辉钣金插件\application\StandardPartsLibrary.dll，用户原有 model3 可立即打开参数窗口，长度下拉正确显示 20/50/80/100/150，宽高及其余已定义项正确显示；取消后 DLL 正常卸载。用户自建说明新增插入选参流程，并同步到标准库根目录和源平头库包，相关说明文件清单哈希已更新。

## 历史部署：用户自建有参库与平头内六角螺钉（2026-09-07 20:31）

- 图档管理新增“用当前部件新建有参”和“选择 PRT 新建有参”。读取已保存原生 PRT 数值表达式，用户可配置 1–12 个表达式、中文标签、数值/下拉/固定/公式联动，创建分类、零件族与首规格。已有族可重新定义参数；另存规格更新原生模型并保留源文件。引用已有公式的参数不可改为可编辑输入；单位沿用表达式原单位。
- 实际文件使用“零件族_规格.prt”；平头预置库使用 GB70_3_M…，避免与圆柱头螺钉同名。已加载同名文件必须再核对完整路径，冲突时拒绝读取/关闭该部件。窗口注册前清理本模块旧类，卸载清理管理、定位、参数和定义窗口类，主窗销毁同时关闭子窗，防止 DLL 复载遗留窗口过程。
- 参考本机燕秀的中文标签、控件选择、表达式与规格值分层管理方式。不是燕秀 XLSX、表达式脚本和 DimControls 格式兼容实现；明确写入用户说明。
- GB/T 70.3-2023 内六角沉头（平头）增加 M3/M4/M5/M6/M8/M10/M12 共 75 个原生有参 PRT、75 张预览和 7 族参数定义/规格表。含头总长 L、头部直径、内六角、退刀/倒角和螺纹长度公式按已核对数据建立；符号螺纹，不生成实体螺旋牙型。75 份模型包围盒与导出网格闭合、正体积检查通过。
- 全部 5 个目标 Release 构建成功：build-codex/build-user-library.log，输出位于 build-user-library/Release。test-user-library-final.log 的 5 组 NX 外部测试和 test-placement-user-library.log 的 11 组回归通过，覆盖创建/定义/联动另存、同名冲突、源字节不变、无效输入与原子回滚，以及既有捕获、方位、预览、装配、多实体和删除。此前已执行的 Block Styler 两轮模式切换、重复选择、Apply 后继续和 Cancel 记录见 18:33 / 12:04 条目；本次未修改其选择回调。
- 实际 NX2412（PID 25760）使用授权模块和完整防篡改清单验证新 UI：从当前测试 PRT 创建“平头建库回归_20260907”，L 标签改为“螺钉总长”、下拉值 20;30;60；管理参数选择 60，另存“界面长度60”，自动返回新规格且 B=24。重新打开定义回读中文、下拉值和真实公式，并成功保存已有族定义。选择 PRT 入口读出同一模板表达式后取消成功。主界面取消、重新加载同一 Release DLL 后管理窗口立即打开；主窗退出同步清理管理窗口，无可见回调错误或延迟弹窗。
- UI 测试全程使用本轮生成的模板，没有编辑用户工作图档。两个测试规格及定义文件移入 build-codex/native-user-parameters-20260907/created-family；仅移除两条测试索引，剩余内容与安装平头库后基线逐行一致。默认 PRT 与测试模板字节一致；用户钣金1_stp.prt 磁盘 SHA-256 仍为 9FC98A890EC7DFD53A20172815097DBF11A14329AF08A7DD310DC6CF4C311F8A。
- 平头目录安装备份：D:\UG智辉钣金插件\backup\StandardPartsLibrary_flat_catalog_20260907_201820。运行时备份：D:\UG智辉钣金插件\backup\StandardPartsLibrary_user_parameters_20260907_203103，含旧 4 个运行时文件、两份活动清单及源码参考清单。构建和界面回归完成后顺序替换；SHA-256、长度、包命令与源码参考哈希更新，其余授权/防篡改元数据保持不变，无调试符号部署。
- 最终 DLL 为 459264 字节，SHA-256 83ED640CB51BEA17609C712D689EEC9441A8B68D1E6F0FEB2F38AEA78944F3B5，PE checksum 0x71B26，DLL characteristics 0x160。Release MaxSpeed、ZH_PROTECTED_BUILD=1、ASLR/DEP 和三个导出入口保留；四个运行时文件与 Release 和活动清单匹配。记录 build-codex/deploy-user-parameters.json。
- 安装后从正式工具栏启动，核对加载 D:\UG智辉钣金插件\application\StandardPartsLibrary.dll；本地无参 2 条、有参 14 族 / 162 规格，平头 M6 预览、含头总长和自建入口显示正确，正常退出。最终逐文件校验圆柱头 196 份与平头 173 份有效载荷哈希/字节数；总索引 164 条，SHA-256 E25E18E60F4C84DA65BB80F0F7551D6107BA1EC42499BBBC88C7B543A794A534。用户自建说明已放在库根目录。

## 历史部署：分类隔离、参数管理与国标内六角螺钉库（2026-09-07 19:36）

- 本地无参和本地有参分别保存分类、搜索及导航状态；分类计数、标准件系列、规格、全部按钮、图档管理目录、添加和删除操作均限定当前库。对旧索引按照 Lib / LibParam 实际相对目录修正类型；切换库关闭旧图档管理和参数窗口，避免跨库操作。
- 参考本机燕秀管理表中“规格行、参数名、中文标签及选择项”的组织方式，新增独立的 parameter-schema.tsv 和 parameters.tsv。管理参数显示固定值、可修改的标准长度下拉项及保存时联动值；验证后另存新的原生有参 PRT，保留源规格并更新当前系列的参数表和索引。非法固定值、非列表长度、非有限数值、非法路径及文件冲突被拒绝；索引写入失败回滚新 PRT、参数表和内存索引。
- 实测 NX 保存 PRT 会清除 Block Styler 撤销标记，已将参数另存改为先退出并销毁主对话框，再保存，最后自动重开库并定位新规格；避免保存后取消窗口出现撤销标记错误。图档管理抓图复用带重入保护的主界面抓图入口，临时隐藏指定方位，移除显示高亮而保留逻辑选择。
- 按 GB/T 70.1-2008 表 1 创建 M3、M4、M5、M6、M8、M10、M12 共 87 个规格，包含原生表达式、实体特征及符号螺纹。长度与螺纹长度 B 联动；采用公称装配几何，内六角开口保留标准范围内间隙。87 份导出网格均闭合且体积为正，生成对应预览图并目视检查代表规格；未生成实体螺旋牙型。
- Release 最终构建成功：build-param-final.log；两份 DLX 校验通过。test-parameters-final.log 验证分类隔离、源 PRT 字节保持、L=60 / L=12 时实体长度与 B 联动、预验证不创建文件和保留撤销标记、上下文恢复及失败回滚，全部通过。test-placement-params-final.log 的原有捕获、方位、预览、装配、多实体、编辑及删除回归全部通过。
- 实际 NX2412（ugraf PID 24220）验证：两类库切换两轮、管理窗口及参数窗口随库切换关闭、反复选择系列和规格；本地无参仅显示原有 2 条，本地有参显示国标分类下 7 系列 / 87 规格。参数另存 M6 长度 60 得到 B=24；最终延后保存流程另存 M3 长度 25 得到 B=18，自动返回新规格，抓图后取消正常退出，无可见撤销标记错误。最终界面补丁再次验证有参按钮及“当前部件加入库”标签正确。
- 本轮两条界面测试规格及图片已移入 build-codex/native-param-ui-20260907，仅移除本轮测试索引行；两份参数表恢复后与标准库源文件哈希一致。最终库索引为用户原有 2 条加国标 87 条，共 89 条，SHA-256 为 7BF331C10477FA4926501388818D322751841981732316DDD1ABE10BC31FD32B。来源模型磁盘 SHA-256 保持 9FC98A890EC7DFD53A20172815097DBF11A14329AF08A7DD310DC6CF4C311F8A，未保存或关闭用户工作模型；本轮未在用户工作模型中执行螺钉的界面 Apply，插入几何由外部 NX 回归验证。
- 标准库已安装到 D:\UG智辉标准件库\LibParam\国标螺钉；源包清单含 197 个文件，安装目录的 196 个有效载荷文件逐一验证 SHA-256 与字节数，根索引保留原用户条目并合并。库安装前备份在 D:\UG智辉钣金插件\backup\StandardPartsLibrary_GB_catalog_20260907_191629，记录 build-codex/deploy-gb-catalog.json。
- 正式插件备份在 D:\UG智辉钣金插件\backup\StandardPartsLibrary_parameters_20260907_193604，含旧 DLL、两个 DLX、BMP、两份活动清单及源码参考清单。构建和实测完成后顺序覆盖四个运行时文件，更新 SHA-256 / 长度与包命令哈希，其他条目、许可及防篡改元数据保持不变，无调试附属文件部署。
- 最终 DLL：396288 字节，SHA-256 EB488DB5D684E6441F0B2208085A6C8662AAE3E3E9E50D78BED8BD06A0FF83C3，PE checksum 0x63EF1，DLL characteristics 0x160；MaxSpeed、ZH_PROTECTED_BUILD=1、ASLR / DEP 及三个导出入口保留。四份文件与 Release 和活动清单一致，部署记录 build-codex/deploy-param-library.json。
- 安装后从正式工具栏启动，确认实际加载 D:\UG智辉钣金插件\application\StandardPartsLibrary.dll；无参 2 条、有参 87 条、国标预览及参数显示正确。

## 历史部署：入库基准方位与无高亮抓图（2026-09-07 18:33）

- 入库对话框在拾取基准插入点之后增加 NX 原生“指定方位”。拾取点、XYZ 和方位控件原点双向同步；默认 ABS 三轴，导出副本按所选原点和方位归一，来源实体、WCS、工作/显示部件保持不变。拒绝零轴、平行轴及非有限数值，归一化有效轴后建立右手坐标系。
- 抓图清除入库实体、拾取点关联对象和全局选择的显示高亮，不清除逻辑选择，也不在抓图后强制重新高亮。抓图期间隐藏方位块，避免三轴弧线、标签和悬浮数值编辑器进入图片，完成后恢复方位块可见性。程序同步及临时隐藏/恢复均位于重入保护范围，恢复异常写入 NX syslog。
- Release 构建成功，日志 `build-capture-orientation.log`；DLX 验证通过。`test-capture-orientation.log` 全部外部 NX 回归通过；新增非单位方向、三轴循环换向、局部包围盒六个极值、无效方向不产生文件、所选基准到目标插入方位往返、来源/WCS/工作上下文及图片字节保持验证。
- 实际 NX2412（ugraf PID 24220）验证：点击入库立即弹窗；选体后抓图去除橙色高亮，基准点从左锁替换到右锁，原生方位控件翻转方向，手工修改基准 X 为 -120；选体/选点/方位切换两轮，实体由 1 个增选至 2 个，清图/重抓两轮。抓图前保持方位悬浮编辑器打开，实际 PNG 不含编辑器及方位手柄。
- “应用”成功生成两体 PRT 与 111840 字节 PNG，图片 SHA-256 `AE6248DAF30EFB213A2E4D10D79FF98FC32750C2B96EE4C7FAE3DDDF3ADFEA8A` 与应用前临时图片完全一致。应用后原生表单重置，继续选择一个实体、再次抓图后取消，临时目录清理，正常返回主库，选中测试件显示保存的图片。关闭主界面正常卸载 DLL，无可见回调异常或等待卡死。
- 回归 PRT、PNG 和索引保存在 `build-codex/native-orientation-ui-20260907`；只移走本轮 `基准方位回归_20260907`，核对其余四个用户标准件索引与测试前一致后恢复索引。来源模型磁盘 SHA-256 保持 `9FC98A890EC7DFD53A20172815097DBF11A14329AF08A7DD310DC6CF4C311F8A`，未保存或关闭来源模型。
- 备份：`D:\UG智辉钣金插件\backup\StandardPartsCapture_orientation_20260907_183328`，含四个旧运行时文件、两份活动清单和源码参考清单。构建和界面验证完成后顺序部署 DLL、两个 DLX 及 BMP；四文件与 Release 哈希/长度完全一致，活动文件哈希清单与包命令 SHA-256 已更新，其他条目和保护元数据保持不变。
- 最终 DLL：342528 字节，SHA-256 `76EF9EE402AF26EB71F0C0869AB7227D1F6F83795F19FB9EE53D7027D5D1DE78`，PE checksum `0x5A560`，DLL characteristics `0x160`。Release MaxSpeed、`ZH_PROTECTED_BUILD=1`、ASLR/DEP 及三个导出入口保留，无调试附属文件部署。记录见 `build-codex/deploy-capture-orientation.json`。
- 安装后从正式工具栏启动，确认加载 `D:\UG智辉钣金插件\application\StandardPartsLibrary.dll`。点击入库立即打开含方位控件的窗口，选体抓图生成 111725 字节无高亮 PNG；取消清理图片并返回主界面，退出后 DLL 卸载，原四条库索引及安装文件哈希再次核对通过。

## 历史部署：入库时抓取模型图片（2026-09-07 18:08）

- 原生入库对话框增加“抓取图片（当前模型视图）”和“清除图片”。使用 NX ImageExportBuilder 导出当前显示部件的完整视图区为白底 PNG；捕获期间临时移除所选实体高亮，然后恢复。图像包括当前可见模型、标注及坐标标记，不包含对话框；不自动隐藏未选实体或改变模型视角。
- 待保存图片位于独立 GUID 临时目录。重新抓取成功后替换旧图，失败保留旧图；清除和取消清理临时图，成功应用消费当前图片。PNG 与新 PRT 使用同一基名；文件名避让既有 PRT 及同名配图，复制或索引失败回滚本次 PRT/PNG，保留原文件。
- Release 构建成功，日志 `build-capture-image.log`；主界面及入库 DLX 校验通过。`test-capture-image.log` 全部外部 NX 回归通过，新增验证 PNG 字节往返、同名匹配、无旧图复用、孤立配图保留、缺图/复制失败/索引失败回滚；既有几何、预览、放置、编辑和删除回归通过。
- 实际 NX2412（ugraf PID 24220）验证：清除/抓取两轮、再次抓取替换、由 1 个实体增选至 2 个、选体/基准点焦点切换两轮；选中实体后抓图不含橙色高亮。第一次应用生成两体 PRT 和 128342 字节 PNG，PNG SHA-256 `7182BD123C9B11C2EC2E382FE8AA6480CD0AA992F5DCC1A52E66E856987D94AE` 与应用前临时图片一致。应用后继续选择一个实体并无图入库，仅产生第二个 PRT；随后抓图再取消，临时目录清理，返回库列表正常。
- 库列表选中首个测试规格显示刚抓取的模型图片，切换第二规格不复用图片。关闭主界面正常卸载 DLL。本轮无可见回调异常或等待卡死；原生 Apply 重启表单以及保存复制部件时的既有撤销标记诊断同前一条验证记录。
- 测试生成的两份 PRT、PNG 和测试索引保存在 `build-codex/native-image-ui-20260907`；用户原有三个标准件索引逐行核对后恢复，SHA-256 `04AB75EFAE7FDDB4E3C6396289B311C9100C75A074112E0897CB899451DDC58C` 与本轮测试前一致。来源模型磁盘哈希保持 `9FC98A890EC7DFD53A20172815097DBF11A14329AF08A7DD310DC6CF4C311F8A`，没有保存或关闭来源模型。
- 备份：`D:\UG智辉钣金插件\backup\StandardPartsCapture_image_20260907_180831`，含四个原运行时文件、两份活动清单及源码参考清单。完成构建和界面测试后依次部署 DLL、两个 DLX 和 BMP。更新四文件哈希/字节数及两份包清单的命令 SHA-256，其他条目与授权/防篡改元数据保持一致。
- 最终 DLL：337920 字节，SHA-256 `14BEA5A9E4DEF1DE5E495A16B601442BB603DB71223FAF65619D60C81EC98C95`，PE checksum `0x54566`，DLL characteristics `0x160`。Release MaxSpeed、`ZH_PROTECTED_BUILD=1`、ASLR/DEP 及三个导出入口保留；无调试附属文件部署。文件与 Release 输出、活动清单核对通过，记录见 `build-codex/deploy-capture-image.json`。
- 从正式工具栏重新打开，确认加载 `D:\UG智辉钣金插件\application\StandardPartsLibrary.dll`；点击选择体加入库立即弹窗，新增抓图按钮成功生成 79552 字节 PNG，取消清理临时图片、返回主界面并正常退出卸载。安装后验证通过。

## 最新部署：点击入库延迟弹窗与等待卡死修复（2026-09-07 12:04）

- 原跳转把 `WM_CLOSE` 发给外层标题窗口，没有触发 NX Block Styler 内部的取消导航；入库请求因此滞留到用户手动取消。现异步向已验证的 NX 原生 Cancel 按钮所属窗口发送导航通知，主对话框释放后立即执行请求。“编辑标准件 PRT”共用该跳转修复。
- 入库窗口改为 NX 原生 `StandardPartsCapture.dlx` / BlockDialog，删除自定义 Win32 窗口和私有 `GetMessage` 等待循环。名称、分类、规格、选体和基准点统一使用原生块；初始化只配置静态值/过滤器，显示后聚焦，更新与程序清选有重入保护，回调捕获并记录 NX 异常。
- 实际 NX2412（ugraf PID 24220）验证：点击“选择体加入库”立即弹窗，无需手动取消主界面；实体由 1 个增选至 2 个，多次切换选体/拾取点并替换基准点；两次“应用”分别成功生成两体和单体 PRT，应用后继续选体，空选择再次应用无重复导出，取消返回主界面。主界面面中心、圆弧、任意点、点投影面均切换两轮，最后取消正常退出，DLL 卸载。此轮无 Work In Progress 卡死或可见回调异常。
- NX 原生 Apply 会重启表单并重置输入。保存复制部件时的既有关闭部件流程会使 NX 记录 undo mark 重置诊断；两次导出及取消返回均完成。该验证不宣称保留跨部件关闭的 NX 撤销历史。
- 回归生成的 `入库回归_20260907` 两个 PRT 与临时索引保存在 `build-codex/native-ui-capture-20260907`。用户库索引恢复后 SHA-256 为 `41238D774A7DBB873E4491FEC7B3988B59A17DC2D52028026C7636CC5B34099E`，与测试前一致；来源模型磁盘文件哈希前后一致，未保存或关闭来源模型。
- `test-capture-native.log` 的全部外部 NX 回归通过，包括选体去重、基准点归零、WCS/来源保持、同名不覆盖、索引失败回滚、入库后插入往返，以及原有预览、装配、多实体、编辑、删除回归。主界面及新增入库 DLX 校验通过。最终 Release clean rebuild 日志：`build-capture-native-final.log`。
- 备份：`D:\UG智辉钣金插件\backup\StandardPartsCapture_native_20260907_120432`。备份原 DLL/DLX/BMP 和两份活动清单后，顺序部署 DLL、两个 DLX 和 BMP。正式工具栏重新打开后确认实际加载 `D:\UG智辉钣金插件\application\StandardPartsLibrary.dll`，新版入库对话框及中文控件标签正确，取消返回正常。
- 最终 DLL：328192 字节，SHA-256 `4038F069A568BA24E8D2433FBE3BAE24BC8C62B1C418E842A1E4CF00D85CED67`，PE checksum `0x51216`，DLL characteristics `0x160`（ASLR/DEP）。Release MaxSpeed、`ZH_PROTECTED_BUILD=1` 和三个导出入口保留；无调试附属文件部署。
- 四个运行时文件均与 Release 输出及 `file-hashes.json` 的哈希/字节数一致；活动包清单及源码部署参考增加入库 DLX 依赖并更新 DLL 哈希，授权与防篡改元数据核对无其他变化。部署核对记录：`build-codex/deploy-capture-native.json`。

## 最新部署：入库选体交互访问锁修复（2026-09-06 17:02）

- 用户明确要求“覆盖”。确认 UG PID 23848 中 StandardPartsLibrary.dll 已卸载后，重新完成 Release 构建、DLX 检查及全部外部 NX 数据/锁生命周期回归，均通过。
- 原 DLL/DLX/BMP 和两份活动清单已备份到 `D:\UG智辉钣金插件\backup\StandardPartsLibrary_20260906_170220_capture_selection_lock`，备份哈希核对后覆盖部署目录三个文件。
- 部署 DLL 为 328192 字节，SHA-256 `2320EA70C5BCEA3E2FEEC904BAA956BA863B2AF21D40CAB0F1F8A934F4717312`。三个运行时文件与 Release 输出及哈希清单一致；两份部署清单和源码部署参考已同步，其余条目及授权/防篡改元数据未改。
- PE checksum `59968`，ASLR/DEP、MaxSpeed、ZH_PROTECTED_BUILD 保留，授权模块存在，无调试附属文件。实际界面选体/换选/取消及拾取点仍待验证；覆盖请求不视为界面验收已通过。

## 实现与测试记录：入库选体交互访问锁修复（2026-09-06）

- 用户报告“选择添加体入库无法选择体”。核对本机 NX2412 SDK `UGOPEN/uf_ui.h` 2489–2557：自定义窗口调用交互 UF API 必须成对调用 `UF_UI_lock_ug_access` / `UF_UI_unlock_ug_access`，且仅在返回 `UF_UI_LOCK_SET` 时继续。原 CapturePick 只有 Win32 隐藏/启用窗口，没有该锁，属于明确接口使用缺陷；尚不能认定为本次所有现象的唯一原因。
- 新增 CaptureUiAccess：选体和拾取点共用，正常、取消、异常路径均释放本次锁；已有锁或锁失败时停止，不解锁其他命令。添加重入保护和选体返回值/数量日志，不改选体范围、实体过滤及入库几何逻辑。
- Release、DLX 校验及全部外部 NX 回归通过。新增注入模拟访问函数测试：连续两次选择的锁计数、异常释放、已有锁及错误锁拒绝；这些是锁生命周期逻辑测试，不是实际界面拾取测试。
- 待部署 DLL：328192 字节；SHA-256 `2320EA70C5BCEA3E2FEEC904BAA956BA863B2AF21D40CAB0F1F8A934F4717312`；非零 PE checksum `59968`，ASLR/DEP 保留。
- computer-use 观察到原版入库窗口、已入库“锁”的状态以及 NX 工作进行中窗口。点击选择实体、激活后重新观察并重试，未观察到选体窗口或焦点变化，随后停止输入；未操作加入库、未删除或保存用户模型。不能据此证明新版本界面修复通过。
- UG PID 23848 仍加载部署目录 StandardPartsLibrary.dll（SHA-256 `5CCBD51048EFAB99B57C82C0D0787FFB28ED70C1BD5E716193D835CD47B428D5`）。尚未覆盖或修改部署清单，需关闭入库及标准件库窗口释放 DLL，再备份/部署和实测。

## 最新部署：选择体加入标准件库（2026-09-06 16:01）

- 用户在获知界面实测未完成、询问是否先行覆盖后回复“已经退出了”；部署前确认没有 ugraf 进程，据此完成覆盖，不将界面测试记为通过。
- 再次完成 Release 构建、对话框配置检查及外部 NX 全部数据回归，均通过，包括选择体导出、基准点归一、同名不覆盖、索引失败回滚与重新插入。
- 原 DLL、DLX、BMP 和两份活动清单已备份到 `D:\UG智辉钣金插件\backup\StandardPartsLibrary_20260906_160107_body_capture`，备份哈希一致后替换三个运行时文件。
- 已更新两份部署清单及源码部署参考清单。部署 DLL 为 325120 字节，SHA-256 `5CCBD51048EFAB99B57C82C0D0787FFB28ED70C1BD5E716193D835CD47B428D5`；三个文件与 Release 输出及哈希清单一致。
- PE checksum `55010`，ASLR/DEP、MaxSpeed、ZH_PROTECTED_BUILD 保留；授权模块存在，清单其余条目和授权/防篡改元数据未改，无调试附属文件。
- 当前状态：已部署；实际打开入库窗口、拾取/替换实体与基准点、连续入库及关闭返回标准件库仍待界面验收。

## 实现与测试记录：选择体加入标准件库（2026-09-06）

- 主界面左下新增“选择体 / 加入库”，结束原 Block Styler 对话框并释放预览后进入独立入库窗口。包含库路径、标准件名称、分类、规格、当前工作件实体多选、基准插入点 XYZ 及模型拾取点。关闭后重新打开标准件库列表。
- 本次按本地无参入库：去除特征参数，只复制选中实体；不接受装配发生体、面或其他工作件的实体。同名规格使用递增文件名，不覆盖已有文件。
- 首次测试发现仅移动源 WCS 后导出不能保证 PRT 原点归一；已改为临时使用绝对 WCS 导出，再在新文件内平移几何，使指定基准点为原点。源实体不移动、原 WCS 恢复，不自动保存源工作件。
- 外部 NX 全部回归通过：空选择拒绝、重复实体去重、未选体排除、带旋转/偏移原 WCS 时基准点归一、原体包围盒及 WCS/工作显示部件保持、同名不覆盖、索引锁定失败回滚、重新插入后基准点准确对齐。原有预览、装配、多实体、右键删除、编辑 PRT 回归也通过。
- Release DLL：325120 字节；SHA-256 `5CCBD51048EFAB99B57C82C0D0787FFB28ED70C1BD5E716193D835CD47B428D5`；PE checksum `55010`；ASLR/DEP 保留；DLX/BMP 未改。
- computer-use 实际验证尝试：文件菜单点击报告 `point (40, 40) is over explorer.exe "FolderView", not target window ugraf.exe "NX - 建模"`；激活、重新观察并重试一次后仍未出现文件菜单。按技能规则停止输入；没有进入新版入库窗口，没有完成实际拾取/换选/入库/关闭回归。
- 部署前安装目录为上一版 DLL `095E5859196E60279502C958F883745BF1F312012617E0910411D5283E8D5A6C`；随后用户回复“已经退出了”，覆盖结果见上方最新部署记录。

## 最新部署：右键删除及编辑 PRT（2026-09-06 14:42）

- 用户在获知完整界面测试尚未完成后明确回复“可以部署了”，据此先行部署，不将界面回归记录为通过。
- 重新完成 Release 构建、对话框配置检查及外部 NX 插入/预览/删除回滚/编辑 PRT 回归，全部通过。
- 部署前确认 UG 内没有加载 StandardPartsLibrary.dll；备份原 DLL、DLX、BMP 和两份活动清单至 `D:\UG智辉钣金插件\backup\StandardPartsLibrary_20260906_144254_context_menu`，备份哈希校验通过。
- 已覆盖 `D:\UG智辉钣金插件\application` 内三个运行时文件；DLL 为 290816 字节，SHA-256 `095E5859196E60279502C958F883745BF1F312012617E0910411D5283E8D5A6C`。
- 两份部署清单及源码部署参考清单已同步；部署文件哈希/字节数与 Release 输出和清单一致，PE checksum `48EA4`，ASLR/DEP、授权及防篡改元数据保留，无调试附属文件。
- 当前状态：右键删除及编辑 PRT 已部署，实际菜单交互、确认框及关闭后切换工作部件仍待界面验收。

## 部署前实现与测试记录：右键删除及编辑 PRT（2026-09-06）

- 新增标准件列表右键菜单：编辑标准件 PRT、删除当前规格；支持键盘菜单定位，空白处不使用旧选择，右键同一行保留当前规格。
- 删除前确认名称、规格及相对路径，默认“否”。未被其他规格共用的模型及全部同名 PNG/JPG/JPEG/BMP 移到库内独立 `backup/deleted_*`，记录 `restore.tsv`；拒绝库外路径，索引失败恢复文件和条目。
- 测试中发现扫描目录与索引路径分隔符差异会重复列项，已统一规范化相对路径比较。
- 编辑请求验证自己的 Block Styler 父窗口并投递关闭消息，退出 Launch 且销毁对话框、计时器和临时预览后再打开源 PRT；不关闭或保存原工作部件，已加载源直接复用。
- Release 和外部 NX 测试通过：原有插入/预览、过滤后右键行映射、当前规格保留、空白/非法行拒绝、模型及多配图备份、刷新不复活条目、共用模型保留、库外路径拒绝、索引锁定失败回滚、编辑源激活/原部件保留/重复打开复用。删除测试仅操作新建临时测试库，没有删除用户标准件。
- 待部署 DLL：290816 字节，SHA-256 `095E5859196E60279502C958F883745BF1F312012617E0910411D5283E8D5A6C`，PE checksum `48EA4`，ASLR/DEP、MaxSpeed、ZH_PROTECTED_BUILD 已验证；DLX/BMP 未更改。
- computer-use 实际界面检查时 UG 进入“添加可重用组件”命令，未获得新版右键菜单交互证据。已暂停输入并请求用户结束当前命令后交接测试；尚未替换 D 盘运行时或更新清单。
- 仍需在临时库实测：打开、动态模式各切换两次、替换输入、应用不关闭、取消/关闭；右键确认框取消不删除；编辑命令正常关闭本对话框后打开正确 PRT。不得将外部测试写成 UI 交互已通过。

## 最新部署状态（2026-09-06 14:27，复杂轮廓预览修复）

- 日志 `Administratorca852d7.syslog` 的 14:23:31、14:23:43 两次弹窗明确为“标准件轮廓过于复杂”：此前源轮廓超过 8192 段直接抛错，另有总绘制 5000 段的拒绝分支。
- 改为统一 5000 段绘制预算：复杂源使用各实体的外形框，过多实体合并为整体框；阵列超预算采用每个位置的外形框，超大阵列采用整体框。不会截断后续实体/位置，也不会无上限增加临时显示调用。
- 只改预览几何提取、预算处理及状态提示；没有改选择、焦点、对话框回调生命周期或正式插入实现。简化框不是精确轮廓，正式插入仍使用完整模型。
- Release 构建、配置校验、装配/多实体插入、确认状态、曲线提取、100/1000 位置预算回归均通过。
- 对日志实际报错的 `models/AAA_890311500_130696.prt` 和 `Lib/用户自定义/BSZ4463.15.05.07.001_Default_按加工_/默认.prt` 只读测试：各生成 864 段简化预览，无目标实体/组件，不切换工作/显示部件，释放自行加载的源部件；两个源文件测试前后 SHA-256 均未变。
- NX 进程仍运行，但确认标准件库 DLL 已卸载。备份三个运行时文件及两份清单到 `D:\UG智辉钣金插件\backup\StandardPartsLibrary_20260906_142715_bounded_preview` 后完成覆盖。
- DLL：269312 字节，SHA-256 `2EFEB36FE7C051E650B9AB23A6920EB368A905A43738374D92E3D52DB8E4B7CD`，PE checksum `434CE`，ASLR/DEP 保留；两份部署清单及部署参考清单已同步，部署哈希与 Release 输出一致，无调试附属文件。
- 以上为真实源文件的预览数据测试，不代表实际屏幕绘制和完整交互验收已通过。

## 上次部署状态（2026-09-06 12:39）

- 用户在获知界面测试尚未完成后明确要求：“我退出UG了，你覆盖吧”。据此先行覆盖，界面回归仍待验证，不记为通过。
- 确认 `ugraf.exe` 已退出；重新执行 Release 构建、对话框配置检查及外部 NX 插入/预览数据回归，均通过。
- 已备份原 DLL、DLX、BMP 和两份活动清单至 `D:\UG智辉钣金插件\backup\StandardPartsLibrary_20260906_123935_preview_fix`，复制前核对备份哈希一致。
- 已替换 `D:\UG智辉钣金插件\application` 内上述三个文件；DLL、DLX 哈希为下文记录的新版值。
- 已更新 `manifest/file-hashes.json` 对应 SHA-256 和字节数、`manifest/zhihui-package.json` 对应命令 SHA-256，以及源码目录的部署参考清单。
- 部署后逐项校验三个文件与 Release 输出及哈希清单一致；非零 PE checksum `43DDC`，ASLR/DEP 保留，无调试附属文件；授权入口、授权模块与防篡改策略未修改。
- 当前状态：已覆盖部署，可从原菜单启动新版；实际界面选点、预览、应用和取消流程尚未完成验收。

## 已完成

- 修复前：选点回调只更新坐标，不调用插入；应用回调没有传递插入失败结果。
- 2026-09-06 用户反馈确认按钮置灰；发现 DLX 三个选择控件均为 Required，包括隐藏辅助选择及可选修剪目标。现在三个控件设为 Optional，由只读 EnableOK 回调统一判定是否已有有效位置。
- 根据用户补充，操作改为选点后显示临时线框预览，确定/应用才正式插入；已应用的位置只消费一次。预览不是组件或实体，不加入特征树。
- 预览从静默加载的来源实体提取边缘，直线保留端点、曲线分段；缓存数据，按位置、方位和阵列变换。取消、正式插入、替换输入时清除临时显示。
- 预览不模拟自动修剪；无实体边缘、过于复杂或大型阵列超出绘制预算时提示无法预览，仍可正式插入。
- 默认使用任意点模式，在 dialogShown 阶段设置过滤和焦点。
- 装配调用改为整个部件引用集，不再硬编码 MODEL。
- Release 编译通过（NX SDK NXException 头文件原有 C4275 警告）。
- 直接使用生产插入函数的外部 NX 测试通过：两次装配实例数为 2，位置为 (20,40,3)、(60,40,3)，引用集不是 MODEL。
- 同一测试中的多实体调用通过：导入实体数为 2，包围盒位置与两个插入点一致。
- 确认状态测试通过：未显示、无选点、有效选点、清空选点、应用后继续、再次选点、关闭。
- 预览数据测试通过：立方体 12 条边，旋转/平移/单位换算正确；静默读取不改变工作/显示部件，不在目标零件创建对象，并释放自行加载的来源零件。圆柱两个圆边缘分段测试通过。
- `tests/ValidateDialog.ps1` 检查三个选择块的 StepStatus 以及 Enable OK/Apply 回调开关，通过。
- 最终测试输出目录：`C:\Users\Administrator\AppData\Local\Temp\ZhihuiPreview_20260906_final`。
- Release DLL：261632 字节；SHA-256 `AD34B44081A8EA8D99C7E58B116CCBE8CEC6F2B43C4EE0E131D122934FF6092D`；非零 PE checksum `43DDC`；ASLR、DEP、MaxSpeed、ZH_PROTECTED_BUILD 已确认；授权入口源代码未修改。
- Release DLX SHA-256 `5DE3743EAF488842CED1C85E8D2961B0D16550C185DA7E7EA2E91CDD9FF117D8`，与源码资源一致；BMP 未更改。

## 覆盖部署之前的阻塞记录（历史）

### 最新加载路径核查

用户再次反馈“还是一样”后，读取 `C:\Users\Administrator\AppData\Local\Temp\Administrator5f68cd19.syslog`：最近的 StandardPartsLibrary 菜单启动记录（例如行 5716–5724）均加载 `D:\UG智辉钣金插件\application\StandardPartsLibrary.dll` 和同目录 DLX，即尚未替换的旧版，而非 build-codex 中的新版。旧版行为不能用于判定新版修复通过或失败。

本轮重置了桌面控制 JavaScript 会话并重新绑定 NX，能够截图和激活窗口，但 Ctrl+O 和文件菜单点击均未产生预期界面变化。没有获得新版实际选点、预览、确认或取消测试的证据；没有继续改运行时代码，也没有部署。

Windows 解锁后用户确认 NX 手动操作正常，但自动化激活仍失败（`failed to activate captured window`），未完成实际窗口内的回归。用户正在协助测试新版 DLL。上述数据测试不包含屏幕实际渲染效果。

按照仓库 AGENTS.md，仍需完成以下对话框回归后才能备份、更新部署文件和保护清单：

- 打开对话框；每个动态模式切换两遍。
- 反复选择和替换输入；验证选点后出现线框预览且确认可用，清空选择清除预览但不创建对象。
- 装配和多实体模式分别连续放置。
- 应用不关闭；已消费的位置不重复放置；继续选点可再放置。
- 取消/关闭清除预览、不插入模型，重新打开正常；旋转/缩放视图后预览恢复显示。

以上阻塞阶段安装目录的 DLL 为旧版 SHA-256 `CF67F9EC07EAA55119D2CC3610585A640FB8062F29CEBCFE8D8AF677655C788F`。后续已按用户明确要求覆盖，见本文开头的最新部署状态；交互测试仍未完成。
