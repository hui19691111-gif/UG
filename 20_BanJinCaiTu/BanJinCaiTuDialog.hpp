//==============================================================================
//  WARNING!!  This file is overwritten by the Block Styler while generating
//  the automation code. Any modifications to this file will be lost after
//  generating the code again.
//
//       Filename:  D:\UGPluginRepo\BanJinCaiTu\BanJinCaiTuDialog.hpp
//
//        This file was generated by the NX Block Styler
//        Created by: Administrator
//              Version: NX 2412
//              Date: 04-13-2026  (Format: mm-dd-yyyy)
//              Time: 10:32
//
//==============================================================================

#ifndef BANJINCAITUDIALOG_H_INCLUDED
#define BANJINCAITUDIALOG_H_INCLUDED

//------------------------------------------------------------------------------
//These includes are needed for the following template code
//------------------------------------------------------------------------------
#include <uf_defs.h>
#include <uf_modl_types.h>
#include <uf_ui_types.h>
#include <uf_retiring.h>
#include <iostream>
#include <vector>
#include <NXOpen/Session.hxx>
#include <NXOpen/UI.hxx>
#include <NXOpen/NXMessageBox.hxx>
#include <NXOpen/Callback.hxx>
#include <NXOpen/NXException.hxx>
#include <NXOpen/NXObjectManager.hxx>
#include <NXOpen/ListingWindow.hxx>
#include <NXOpen/Direction.hxx>
#include <NXOpen/DirectionCollection.hxx>
#include <NXOpen/BodyDumbRule.hxx>
#include <NXOpen/FaceDumbRule.hxx>
#include <NXOpen/Features_AssociativeArcBuilder.hxx>
#include <NXOpen/Features_BaseFeatureCollection.hxx>
#include <NXOpen/Features_BooleanBuilder.hxx>
#include <NXOpen/Features_DatumAxisBuilder.hxx>
#include <NXOpen/Features_FeatureCollection.hxx>
#include <NXOpen/Features_OffsetFaceBuilder.hxx>
#include <NXOpen/GeometricUtilities_BooleanRegionSelect.hxx>
#include <NXOpen/NXObject.hxx>
#include <NXOpen/Point.hxx>
#include <NXOpen/PointCollection.hxx>
#include <NXOpen/Plane.hxx>
#include <NXOpen/PlaneCollection.hxx>
#include <NXOpen/PartCollection.hxx>
#include <NXOpen/ScCollector.hxx>
#include <NXOpen/ScCollectorCollection.hxx>
#include <NXOpen/ScRuleFactory.hxx>
#include <NXOpen/SelectionIntentRule.hxx>
#include <NXOpen/SelectionIntentRuleOptions.hxx>
#include <NXOpen/SelectPoint.hxx>
#include <NXOpen/SelectNXObjectList.hxx>
#include <NXOpen/BlockStyler_UIBlock.hxx>
#include <NXOpen/BlockStyler_BlockDialog.hxx>
#include <NXOpen/BlockStyler_PropertyList.hxx>
#include <NXOpen/BlockStyler_Group.hxx>
#include <NXOpen/BlockStyler_SelectObject.hxx>
#include <NXOpen/Body.hxx>
#include <NXOpen/BodyCollection.hxx>
#include <NXOpen/DisplayableObject.hxx>
#include <NXOpen/Face.hxx>
#include <NXOpen/TaggedObject.hxx>
#include <NXOpen/Update.hxx>
#include <NXOpen/ugmath.hxx>
#include <vector>

//------------------------------------------------------------------------------
//Bit Option for Property: SnapPointTypesEnabled
//------------------------------------------------------------------------------
#define              SnapPointTypesEnabled_UserDefined (1 << 0);
#define                 SnapPointTypesEnabled_Inferred (1 << 1);
#define           SnapPointTypesEnabled_ScreenPosition (1 << 2);
#define                 SnapPointTypesEnabled_EndPoint (1 << 3);
#define                 SnapPointTypesEnabled_MidPoint (1 << 4);
#define             SnapPointTypesEnabled_ControlPoint (1 << 5);
#define             SnapPointTypesEnabled_Intersection (1 << 6);
#define                SnapPointTypesEnabled_ArcCenter (1 << 7);
#define            SnapPointTypesEnabled_QuadrantPoint (1 << 8);
#define            SnapPointTypesEnabled_ExistingPoint (1 << 9);
#define             SnapPointTypesEnabled_PointonCurve (1 <<10);
#define           SnapPointTypesEnabled_PointonSurface (1 <<11);
#define         SnapPointTypesEnabled_PointConstructor (1 <<12);
#define     SnapPointTypesEnabled_TwocurveIntersection (1 <<13);
#define             SnapPointTypesEnabled_TangentPoint (1 <<14);
#define                    SnapPointTypesEnabled_Poles (1 <<15);
#define         SnapPointTypesEnabled_BoundedGridPoint (1 <<16);
#define         SnapPointTypesEnabled_FacetVertexPoint (1 <<17);
#define            SnapPointTypesEnabled_DefiningPoint (1 <<18);
//------------------------------------------------------------------------------
//Bit Option for Property: SnapPointTypesOnByDefault
//------------------------------------------------------------------------------
#define             SnapPointTypesOnByDefault_EndPoint (1 << 3);
#define             SnapPointTypesOnByDefault_MidPoint (1 << 4);
#define         SnapPointTypesOnByDefault_ControlPoint (1 << 5);
#define         SnapPointTypesOnByDefault_Intersection (1 << 6);
#define            SnapPointTypesOnByDefault_ArcCenter (1 << 7);
#define        SnapPointTypesOnByDefault_QuadrantPoint (1 << 8);
#define        SnapPointTypesOnByDefault_ExistingPoint (1 << 9);
#define         SnapPointTypesOnByDefault_PointonCurve (1 <<10);
#define       SnapPointTypesOnByDefault_PointonSurface (1 <<11);
#define     SnapPointTypesOnByDefault_PointConstructor (1 <<12);
#define     SnapPointTypesOnByDefault_BoundedGridPoint (1 <<16);
#define     SnapPointTypesOnByDefault_FacetVertexPoint (1 <<17);
//------------------------------------------------------------------------------
// Namespaces needed for following template
//------------------------------------------------------------------------------
using namespace std;
using namespace NXOpen;
using namespace NXOpen::BlockStyler;

class DllExport BanJinCaiTuDialog
{
    // class members
public:
    static Session *theSession;
    static UI *theUI;
    BanJinCaiTuDialog();
    ~BanJinCaiTuDialog();
    NXOpen::BlockStyler::BlockDialog::DialogResponse Launch();
    
    //----------------------- BlockStyler Callback Prototypes ---------------------
    // The following member function prototypes define the callbacks 
    // specified in your BlockStyler dialog.  The empty implementation
    // of these prototypes is provided in the BanJinCaiTuDialog.cpp file. 
    // You are REQUIRED to write the implementation for these functions.
    //------------------------------------------------------------------------------
    void initialize_cb();
    void dialogShown_cb();
    int apply_cb();
    int ok_cb();
    int update_cb(NXOpen::BlockStyler::UIBlock* block);
    PropertyList* GetBlockProperties(const char *blockID);
    
private:
    struct FaceInfo
    {
        tag_t tag;
        int faceType;
        bool isPlanar;
        double point[3];
        double normal[3];
        double box[6];
        double radius;
    };

    struct TraversalContext
    {
        double bodyCenter[3];
        double seedSideScore;
        double thickness;
        tag_t referenceEdgeTag;
        double referenceEdgeDirection[3];
        NXOpen::ListingWindow* listingWindow;
    };

    struct SharedEdgeStats
    {
        int totalSharedEdges;
        int linearSharedEdges;
        int thicknessEdges;
        int parallelQualifiedEdges;
        tag_t firstParallelQualifiedEdgeTag;
    };

    struct TraversalNode
    {
        tag_t faceTag;
        tag_t incomingEdgeTag;
    };

    struct RecognizedFaceRegion
    {
        NXOpen::Face* face;
        tag_t incomingEdgeTag;
        tag_t outgoingEdgeTag;
    };

    struct RightAngleFacePair
    {
        tag_t firstFaceTag;
        tag_t secondFaceTag;
        tag_t sharedEdgeTag;
        std::size_t firstIndex;
        std::size_t secondIndex;
    };

    std::vector<RecognizedFaceRegion> CollectContinuousBendFaces(
        NXOpen::Face* seedFace,
        NXOpen::Face* stopFace) const;
    FaceInfo QueryFaceInfo(tag_t faceTag) const;
    TraversalContext BuildTraversalContext(NXOpen::Face* seedFace) const;
    std::vector<tag_t> UfListToTags(uf_list_p_t list) const;
    double ComputeAngleDegrees(const double lhs[3], const double rhs[3]) const;
    double DistancePointToSegment(const double point[3], const double start[3], const double end[3]) const;
    double ComputeEdgeMidpointDistance(tag_t firstEdgeTag, tag_t secondEdgeTag) const;
    bool TryGetLinearEdgeDirection(tag_t edgeTag, double direction[3]) const;
    tag_t FindNearestReferenceEdge(tag_t faceTag, const NXOpen::Point3d& pickPoint, double thickness) const;
    std::vector<tag_t> CollectReferenceEdgeNeighborFaces(tag_t seedFaceTag, tag_t referenceEdgeTag) const;
    bool IsBendReferenceEdge(tag_t seedFaceTag, tag_t edgeTag, double thickness) const;
    bool IsThicknessEdge(tag_t edgeTag, double thickness) const;
    SharedEdgeStats AnalyzeSharedEdges(
        tag_t currentFaceTag,
        tag_t candidateFaceTag,
        const double referenceDirection[3],
        double thickness) const;
    tag_t FindClosestOutgoingChainEdge(
        tag_t currentFaceTag,
        const double referenceDirection[3],
        double thickness,
        tag_t incomingEdgeTag) const;
    std::vector<tag_t> CollectOutgoingChainEdges(
        tag_t currentFaceTag,
        const double referenceDirection[3],
        double thickness,
        tag_t incomingEdgeTag) const;
    bool TrimExtractedFaceToInnerBand(
        tag_t sourceFaceTag,
        tag_t extractedSheetBodyTag,
        tag_t incomingEdgeTag,
        tag_t outgoingEdgeTag) const;
    bool AreDirectionsParallel(const double lhs[3], const double rhs[3], double toleranceDegrees) const;
    double ComputeSideScore(const FaceInfo& faceInfo, const double bodyCenter[3]) const;
    double EstimateThickness(NXOpen::Face* seedFace) const;
    double ComputePlanarShortSpan(const FaceInfo& faceInfo) const;
    bool ShouldStopAtNextFaceByThicknessEdge(
        tag_t candidateFaceTag,
        tag_t referenceBendEdgeTag,
        double thickness) const;
    bool HasOppositePlanarAdjacentFaces(tag_t faceTag) const;
    bool IsThicknessSideFace(const FaceInfo& faceInfo, double thickness) const;
    bool IsTransitionBendFace(const FaceInfo& faceInfo, double thickness) const;
    bool IsContinuousBendNeighbor(
        tag_t currentFaceTag,
        tag_t candidateFaceTag,
        const TraversalContext& context) const;
    void LogTraversalDecision(
        tag_t currentFaceTag,
        tag_t candidateFaceTag,
        const SharedEdgeStats& edgeStats,
        double bendAngle,
        const char* decision) const;
    void HighlightFaces(const std::vector<NXOpen::Face*>& faces) const;
    std::vector<tag_t> CollectAdjacentRightAngleFaceTags(
        const std::vector<RecognizedFaceRegion>& faceRegions,
        const std::vector<tag_t>& extractedFaceTags) const;
    std::vector<RightAngleFacePair> CollectAdjacentRightAngleFacePairs(
        const std::vector<RecognizedFaceRegion>& faceRegions,
        const std::vector<tag_t>& extractedFaceTags) const;
    void ColorObjectsRed(const std::vector<tag_t>& objectTags) const;
    void DrawFaceNormals(const std::vector<tag_t>& faceTags, double length) const;
    void DrawOrderedNormalSegments(
        const std::vector<RecognizedFaceRegion>& faceRegions,
        const std::vector<tag_t>& extractedSheetBodyTags) const;
    double ComputeConvergentFaceAngleDegrees(
        const FaceInfo& currentInfo,
        const FaceInfo& nextInfo,
        tag_t sharedEdgeTag) const;
    void ComputeNormalSegmentPoints(
        const FaceInfo& currentInfo,
        const FaceInfo& nextInfo,
        tag_t sharedEdgeTag,
        double currentStartPoint[3],
        double nextStartPoint[3],
        double currentEndPoint[3],
        double nextEndPoint[3]) const;
    void ComputeNormalDistancePair(
        const FaceInfo& currentInfo,
        const FaceInfo& nextInfo,
        tag_t sharedEdgeTag,
        double& startDistance,
        double& endDistance) const;
    void PrintOrderedFaceAngles(
        const std::vector<RecognizedFaceRegion>& faceRegions,
        const std::vector<tag_t>& extractedFaceTags,
        NXOpen::ListingWindow* listingWindow) const;
    std::vector<tag_t> CollectBodyFaceTags(tag_t bodyTag) const;
    std::vector<tag_t> CollectSheetFaceTags(const std::vector<tag_t>& sheetBodyTags) const;
    tag_t FindSharedEdgeBetweenFaces(tag_t currentFaceTag, tag_t nextFaceTag) const;
    bool AreEdgesCollinearAndOverlapping(tag_t firstEdgeTag, tag_t secondEdgeTag) const;
    std::vector<tag_t> FindThicknessFacesConnectedToBendEdge(
        tag_t thickenedBodyTag,
        tag_t bendEdgeTag,
        double thickness) const;
    void OffsetFacesByDistance(
        const std::vector<tag_t>& faceTags,
        double distance) const;
    void OffsetRightAngleThicknessFaces(
        const std::vector<RightAngleFacePair>& rightAnglePairs,
        const std::vector<tag_t>& thickenedBodyTags,
        double thickness) const;
    bool ComputeBisectorArcData(
        const FaceInfo& currentInfo,
        const FaceInfo& nextInfo,
        tag_t sharedEdgeTag,
        double startPoint[3],
        double endPoint[3],
        double& arcDegrees) const;
    bool ComputeBisectorArcPoints(
        const FaceInfo& currentInfo,
        const FaceInfo& nextInfo,
        tag_t sharedEdgeTag,
        double centerPoint[3],
        double bisectorPoint[3],
        double face1Point[3],
        double face2Point[3],
        double& arcDegrees,
        double& face1AngleDegrees,
        double& bisectorAngleDegrees,
        double& face2AngleDegrees) const;
    bool FindNearestCurveFaceIntersection(
        tag_t curveTag,
        tag_t faceTag,
        const double referencePoint[3],
        double intersectionPoint[3]) const;
    bool ComputeActualArcDegreesForPair(
        tag_t currentFaceTag,
        tag_t nextFaceTag,
        tag_t currentExtractedFaceTag,
        tag_t nextExtractedFaceTag,
        double& actualArcDegrees) const;
    double ComputeArcDegreesFromPoints(
        const double centerPoint[3],
        const double face1Point[3],
        const double bisectorPoint[3],
        const double face2Point[3],
        const double edgeDirection[3]) const;
    bool BuildProbeSegmentOnFace(
        tag_t sourceFaceTag,
        tag_t targetFaceTag,
        tag_t sharedEdgeTag,
        double startPoint[3],
        double endPoint[3]) const;
    bool DoesProbeHitFace(
        tag_t sourceFaceTag,
        tag_t targetFaceTag,
        tag_t sharedEdgeTag) const;
    std::vector<tag_t> ExtractRecognizedFaces(
        const std::vector<RecognizedFaceRegion>& faceRegions,
        std::vector<tag_t>* sourceFaceTags) const;
    int DeleteHolesFromExtractedBodies(const std::vector<tag_t>& sheetBodyTags) const;
    int CreateFirstBendSectionCurves(
        tag_t firstBendEdgeTag,
        const std::vector<tag_t>& sheetBodyTags,
        const std::vector<tag_t>& sourceFaceTags,
        double thickness) const;
    std::vector<tag_t> ThickenTrimmedFaces(
        const std::vector<tag_t>& sheetBodyTags,
        double thickness,
        bool alongPositiveNormal) const;
    tag_t UniteBodies(const std::vector<tag_t>& bodyTags) const;
    NXOpen::Body* ResolveLiveBody(tag_t bodyTag) const;
    tag_t IntersectSelectedBodyWithToolBody(tag_t toolBodyTag) const;
    tag_t SubtractSelectedBodyByToolBody(tag_t toolBodyTag) const;
    void RemoveBodyParameters(const std::vector<tag_t>& bodyTags) const;
    void DeleteUnparameterizedBodies(const std::vector<tag_t>& bodyTags) const;
    std::string BuildRegionSummary(
        NXOpen::Face* seedFace,
        const std::vector<RecognizedFaceRegion>& bendFaces,
        const TraversalContext& context) const;

    const char* theDlxFileName;
    NXOpen::BlockStyler::BlockDialog* theDialog;
    NXOpen::BlockStyler::Group* group0;
    NXOpen::BlockStyler::SelectObject* selection0;// Block type: Selection
    NXOpen::BlockStyler::SelectObject* selection1;// Block type: Selection
    NXOpen::Point3d selectedPickPoint;
    NXOpen::Face* selectedFace;
    NXOpen::Face* selectedEndFace;
    NXOpen::Body* selectedBody;
    
};
#endif //BANJINCAITUDIALOG_H_INCLUDED
