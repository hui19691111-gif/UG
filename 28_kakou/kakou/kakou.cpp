//==============================================================================
//  WARNING!!  This file is overwritten by the Block UI Styler while generating
//  the automation code. Any modifications to this file will be lost after
//  generating the code again.
//
//       Filename:  D:\LPOpenPrint\application\kakou.cpp
//
//        This file was generated by the NX Block UI Styler
//        Created by: admin
//              Version: NX 2206
//              Date: 02-13-2023  (Format: mm-dd-yyyy)
//              Time: 08:26 (Format: hh-mm)
//
//==============================================================================

//==============================================================================
//  Purpose:  This TEMPLATE file contains C++ source to guide you in the
//  construction of your Block application dialog. The generation of your
//  dialog file (.dlx extension) is the first step towards dialog construction
//  within NX.  You must now create a NX Open application that
//  utilizes this file (.dlx).
//
//  The information in this file provides you with the following:
//
//  1.  Help on how to load and display your Block UI Styler dialog in NX
//      using APIs provided in NXOpen.BlockStyler namespace
//  2.  The empty callback methods (stubs) associated with your dialog items
//      have also been placed in this file. These empty methods have been
//      created simply to start you along with your coding requirements.
//      The method name, argument list and possible return values have already
//      been provided for you.
//==============================================================================

//------------------------------------------------------------------------------
//These includes are needed for the following template code
//------------------------------------------------------------------------------
#include "kakou.hpp"
#include "UG_TouWenJian.h"
#include "custom_function.h"
#include "custom_function.cpp"
#include <cmath>
using namespace NXOpen;
using namespace NXOpen::BlockStyler;

//------------------------------------------------------------------------------
// Initialize static variables
//------------------------------------------------------------------------------
Session *(kakou::theSession) = NULL;
UI *(kakou::theUI) = NULL;
//------------------------------------------------------------------------------
// Constructor for NX Styler class
//------------------------------------------------------------------------------
kakou::kakou()
{
    try
    {
        // Initialize the NX Open C++ API environment
        kakou::theSession = NXOpen::Session::GetSession();
        kakou::theUI = UI::GetUI();
        theDlxFileName = "kakou.dlx";
        theDialog = kakou::theUI->CreateDialog(theDlxFileName);
        // Registration of callback functions
        theDialog->AddApplyHandler(make_callback(this, &kakou::apply_cb));
        theDialog->AddOkHandler(make_callback(this, &kakou::ok_cb));
        theDialog->AddUpdateHandler(make_callback(this, &kakou::update_cb));
        theDialog->AddInitializeHandler(make_callback(this, &kakou::initialize_cb));
        theDialog->AddDialogShownHandler(make_callback(this, &kakou::dialogShown_cb));
    }
    catch(exception& ex)
    {
        //---- Enter your exception handling code here -----
        throw;
    }
}

//------------------------------------------------------------------------------
// Destructor for NX Styler class
//------------------------------------------------------------------------------
kakou::~kakou()
{
    if (theDialog != NULL)
    {
        delete theDialog;
        theDialog = NULL;
    }
}
//------------------------------- DIALOG LAUNCHING ---------------------------------
//
//    Before invoking this application one needs to open any part/empty part in NX
//    because of the behavior of the blocks.
//
//    Make sure the dlx file is in one of the following locations:
//        1.) From where NX session is launched
//        2.) $UGII_USER_DIR/application
//        3.) For released applications, using UGII_CUSTOM_DIRECTORY_FILE is highly
//            recommended. This variable is set to a full directory path to a file 
//            containing a list of root directories for all custom applications.
//            e.g., UGII_CUSTOM_DIRECTORY_FILE=$UGII_BASE_DIR\ugii\menus\custom_dirs.dat
//
//    You can create the dialog using one of the following way:
//
//    1. USER EXIT
//
//        1) Create the Shared Library -- Refer "Block UI Styler programmer's guide"
//        2) Invoke the Shared Library through File->Execute->NX Open menu.
//
//------------------------------------------------------------------------------
extern "C" DllExport void  ufusr(char *param, int *retcod, int param_len)
{
    kakou *thekakou = NULL;
    try
    {
        thekakou = new kakou();
        // The following method shows the dialog immediately
        thekakou->Launch();
    }
    catch(exception& ex)
    {
        //---- Enter your exception handling code here -----
        kakou::theUI->NXMessageBox()->Show("Block Styler", NXOpen::NXMessageBox::DialogTypeError, ex.what());
    }
    if(thekakou != NULL)
    {
        delete thekakou;
        thekakou = NULL;
    }
}

//------------------------------------------------------------------------------
// This method specifies how a shared image is unloaded from memory
// within NX. This method gives you the capability to unload an
// internal NX Open application or user  exit from NX. Specify any
// one of the three constants as a return value to determine the type
// of unload to perform:
//
//
//    Immediately : unload the library as soon as the automation program has completed
//    Explicitly  : unload the library from the "Unload Shared Image" dialog
//    AtTermination : unload the library when the NX session terminates
//
//
// NOTE:  A program which associates NX Open applications with the menubar
// MUST NOT use this option since it will UNLOAD your NX Open application image
// from the menubar.
//------------------------------------------------------------------------------
extern "C" DllExport int ufusr_ask_unload()
{
    //return (int)Session::LibraryUnloadOptionExplicitly;
    return (int)Session::LibraryUnloadOptionImmediately;
    //return (int)Session::LibraryUnloadOptionAtTermination;
}

//------------------------------------------------------------------------------
// Following method cleanup any housekeeping chores that may be needed.
// This method is automatically called by NX.
//------------------------------------------------------------------------------
extern "C" DllExport void ufusr_cleanup(void)
{
    try
    {
        //---- Enter your callback code here -----
    }
    catch(exception& ex)
    {
        //---- Enter your exception handling code here -----
        kakou::theUI->NXMessageBox()->Show("Block Styler", NXOpen::NXMessageBox::DialogTypeError, ex.what());
    }
}

//------------------------------------------------------------------------------
//This method launches the dialog to screen
//------------------------------------------------------------------------------
NXOpen::BlockStyler::BlockDialog::DialogResponse kakou::Launch()
{
    NXOpen::BlockStyler::BlockDialog::DialogResponse dialogResponse= NXOpen::BlockStyler::BlockDialog::DialogResponse::DialogResponseInvalid;
    try
    {
        dialogResponse=theDialog->Launch();
    }
    catch(exception& ex)
    {
        //---- Enter your exception handling code here -----
        kakou::theUI->NXMessageBox()->Show("Block Styler", NXOpen::NXMessageBox::DialogTypeError, ex.what());
    }
    return dialogResponse;
}

//------------------------------------------------------------------------------
//---------------------Block UI Styler Callback Functions--------------------------
//------------------------------------------------------------------------------

//------------------------------------------------------------------------------
//Callback Name: initialize_cb
//------------------------------------------------------------------------------
void kakou::initialize_cb()
{
    try
    {
        group0 = dynamic_cast<NXOpen::BlockStyler::Group*>(theDialog->TopBlock()->FindBlock("group0"));
        drawingArea0 = dynamic_cast<NXOpen::BlockStyler::DrawingArea*>(theDialog->TopBlock()->FindBlock("drawingArea0"));
        group = dynamic_cast<NXOpen::BlockStyler::Group*>(theDialog->TopBlock()->FindBlock("group"));
        enum0 = dynamic_cast<NXOpen::BlockStyler::Enumeration*>(theDialog->TopBlock()->FindBlock("enum0"));
        linear_dim03 = dynamic_cast<NXOpen::BlockStyler::LinearDimension*>(theDialog->TopBlock()->FindBlock("linear_dim03"));
        linear_dim0 = dynamic_cast<NXOpen::BlockStyler::LinearDimension*>(theDialog->TopBlock()->FindBlock("linear_dim0"));
        linear_dim01 = dynamic_cast<NXOpen::BlockStyler::LinearDimension*>(theDialog->TopBlock()->FindBlock("linear_dim01"));
        linear_dim02 = dynamic_cast<NXOpen::BlockStyler::LinearDimension*>(theDialog->TopBlock()->FindBlock("linear_dim02"));
        selection0 = dynamic_cast<NXOpen::BlockStyler::SelectObject*>(theDialog->TopBlock()->FindBlock("selection0"));
        manip0 = dynamic_cast<NXOpen::BlockStyler::SpecifyOrientation*>(theDialog->TopBlock()->FindBlock("manip0"));

        //设置过滤
        Selection::SelectionAction action = Selection::SelectionActionClearAndEnableSpecific;
        std::vector<Selection::MaskTriple> maskArray(1);
        maskArray[0] = Selection::MaskTriple(UF_solid_type, UF_solid_body_subtype, UF_UI_SEL_FEATURE_ANY_FACE);
        selection0->GetProperties()->SetSelectionFilter("SelectionFilter", action, maskArray);
        
    }
    catch(exception& ex)
    {
        //---- Enter your exception handling code here -----
        kakou::theUI->NXMessageBox()->Show("Block Styler", NXOpen::NXMessageBox::DialogTypeError, ex.what());
    }
}

std::vector<NXOpen::TaggedObject*> edge1_TAG;
NXOpen::Edge* edge1;
NXOpen::Point3d Point3d1;
NXOpen::Point3d Point3d2;
Features::Feature* Feature1;
Features::Feature* Feature2;
Features::Feature* Feature3;
Features::Feature* Feature4;
Features::Feature* Feature5;
Features::Feature* Feature6;
Features::Feature* Feature7;
Features::Feature* Feature8;
vector<Features::Feature*> vFeature1;
NXOpen::Matrix3x3 matrix1;
double Thedim0;
double Thedim01;
double Thedim02;
char Thardim0[256];
char Thardim01[256];
char Thardim02[256];
char Thardim0a[256];
char Thardim01a[256];
char Thardim02a[256];
char Z[256];
char Za[256];
double X_vec[3];
double Y_vec[3];
double Z_vec[3];
double mtx[9];
double HouDu1 = 100;
Body* Body1;
Body* Bodytool;

NXOpen::Point* point_Zon;
//------------------------------------------------------------------------------
//Callback Name: dialogShown_cb
//This callback is executed just before the dialog launch. Thus any value set 
//here will take precedence and dialog will be launched showing that value. 
//------------------------------------------------------------------------------
void kakou::dialogShown_cb()
{
    try
    {

    }
    catch(exception& ex)
    {
        //---- Enter your exception handling code here -----
        kakou::theUI->NXMessageBox()->Show("Block Styler", NXOpen::NXMessageBox::DialogTypeError, ex.what());
    }
}

//------------------------------------------------------------------------------
//Callback Name: apply_cb
//------------------------------------------------------------------------------
int kakou::apply_cb()
{
    int errorCode = 0;
    try
    {
        UF_initialize();

        int num_result=0;
        tag_t* resulting_bodies=NULL;
        if (enum0->GetProperties()->GetEnum("Value") ==0)
        {
            vector<Body*>Vbody11;
            Vbody11 = Feature1->GetBodies();
            vector<Body*>Vbody22;
            Vbody22 = Feature2->GetBodies();
            UF_MODL_unite_bodies(Body1->Tag(), Vbody11[0]->Tag());
            UF_MODL_subtract_bodies(Bodytool->Tag(), Vbody22[0]->Tag(), &num_result, &resulting_bodies);
        }
        if (enum0->GetProperties()->GetEnum("Value") == 1)
        {
            vector<Body*>Vbody0(2);
            vector<Body*>Vbody1(2);

            Vbody0[0] = Feature1->GetBodies()[0]; Vbody0[1] = Feature3->GetBodies()[0];
            Vbody1[0] = Feature2->GetBodies()[0]; Vbody1[1] = Feature4->GetBodies()[0];
            Features::Feature* Feature1a;
            custom_boolean(Body1, Vbody0, Features::Feature::BooleanType::BooleanTypeUnite, Feature1a, false, false);
            custom_boolean(Bodytool, Vbody1, Features::Feature::BooleanType::BooleanTypeSubtract, Feature1a, false, false);
        }
        if (enum0->GetProperties()->GetEnum("Value") == 2)
        {
            vector<Body*>Vbody0(3);
            vector<Body*>Vbody1(3);

            Vbody0[0] = Feature1->GetBodies()[0]; Vbody0[1] = Feature3->GetBodies()[0]; Vbody0[2] = Feature5->GetBodies()[0];
            Vbody1[0] = Feature2->GetBodies()[0]; Vbody1[1] = Feature4->GetBodies()[0]; Vbody1[2] = Feature6->GetBodies()[0];
            Features::Feature* Feature1a;
            custom_boolean(Body1, Vbody0, Features::Feature::BooleanType::BooleanTypeUnite, Feature1a, false, false);
            custom_boolean(Bodytool, Vbody1, Features::Feature::BooleanType::BooleanTypeSubtract, Feature1a, false, false);
        }
        if (enum0->GetProperties()->GetEnum("Value") == 3)
        {
            vector<Body*>Vbody0(4);
            vector<Body*>Vbody1(4);

            Vbody0[0] = Feature1->GetBodies()[0]; Vbody0[1] = Feature3->GetBodies()[0]; Vbody0[2] = Feature5->GetBodies()[0]; Vbody0[3] = Feature7->GetBodies()[0];
            Vbody1[0] = Feature2->GetBodies()[0]; Vbody1[1] = Feature4->GetBodies()[0]; Vbody1[2] = Feature6->GetBodies()[0]; Vbody1[3] = Feature8->GetBodies()[0];

            Features::Feature* Feature1a;
            custom_boolean(Body1, Vbody0, Features::Feature::BooleanType::BooleanTypeUnite, Feature1a, false, false);
            custom_boolean(Bodytool, Vbody1, Features::Feature::BooleanType::BooleanTypeSubtract, Feature1a, false, false);
        }

        UF_terminate();

    }
    catch(exception& ex)
    {
        //---- Enter your exception handling code here -----
        errorCode = 1;
        kakou::theUI->NXMessageBox()->Show("Block Styler", NXOpen::NXMessageBox::DialogTypeError, ex.what());
    }
    return errorCode;
}

//------------------------------------------------------------------------------
//Callback Name: update_cb
//------------------------------------------------------------------------------
int kakou::update_cb(NXOpen::BlockStyler::UIBlock* block)
{
    try
    {
        if(block == drawingArea0)
        {
        //---------Enter your code here-----------
        }
        else if (block == enum0)
        {

            if (vFeature1.size()>0)
            {

                vector<NXOpen::TaggedObject* > objects1;
                for (size_t i = 0; i < vFeature1.size(); i++)
                {

                    objects1.push_back(vFeature1[i]);
                }
                custom_del(objects1);
                goto abcde;
            }

        }
        else if (block == linear_dim03)
        {
            if (vFeature1.size() > 0)
            {
                vector<NXOpen::TaggedObject* > objects1;
                for (size_t i = 0; i < vFeature1.size(); i++)
                {

                    objects1.push_back(vFeature1[i]);
                }
                custom_del(objects1);
                goto abcde;
            }
        }
        else if(block == linear_dim0)
        {
            if (vFeature1.size() > 0)
            {
                vector<NXOpen::TaggedObject* > objects1;
                for (size_t i = 0; i < vFeature1.size(); i++)
                {

                    objects1.push_back(vFeature1[i]);
                }
                custom_del(objects1);
                goto abcde;
            }
        }
        else if(block == linear_dim01)
        {
            if (vFeature1.size() > 0)
            {
                vector<NXOpen::TaggedObject* > objects1;
                for (size_t i = 0; i < vFeature1.size(); i++)
                {

                    objects1.push_back(vFeature1[i]);
                }
                custom_del(objects1);
                goto abcde;
            }
        }
        else if(block == linear_dim02)
        {
            if (vFeature1.size() > 0)
            {
                vector<NXOpen::TaggedObject* > objects1;
                for (size_t i = 0; i < vFeature1.size(); i++)
                {

                    objects1.push_back(vFeature1[i]);
                }
                custom_del(objects1);
                goto abcde;
            }
        }
        else if(block == selection0)
        {
            UF_initialize();
        abcde:
            if (vFeature1.size() > 0)
            {
                vector<NXOpen::TaggedObject* > objects1;
                for (size_t i = 0; i < vFeature1.size(); i++)
                {

                    objects1.push_back(vFeature1[i]);
                }
                custom_del(objects1);
            }

            if (enum0->GetProperties()->GetEnum("Value") !=0)
            {
                linear_dim03->SetShow(true);
                manip0->SetShow(false);
            }
            else
            {
                linear_dim03->SetShow(false);
                manip0->SetShow(true);
            }
            PropertyList* dim0Props1 = linear_dim0->GetProperties();
            Thedim0 = dim0Props1->GetDouble("Value");
            delete dim0Props1;
            dim0Props1 = NULL;
            manip0->SetVisibleManipulatorHandles(8);

            PropertyList* dim01Props1 = linear_dim01->GetProperties();
            Thedim01 = dim01Props1->GetDouble("Value");
            delete dim01Props1;
            dim01Props1 = NULL;


            PropertyList* dim02Props1 = linear_dim02->GetProperties();
            Thedim02 = dim02Props1->GetDouble("Value");
            delete dim02Props1;
            dim02Props1 = NULL;

            sprintf(Thardim0, "%.2f", Thedim0);
            sprintf(Thardim0a, "%.2f", Thedim0 + Thedim02 * 2);
            sprintf(Thardim01, "%.2f", Thedim01);
            sprintf(Thardim01a, "%.2f", Thedim01 + Thedim02 * 2);
            sprintf(Thardim02, "%.2f", Thedim02);

            if (edge1_TAG.size()>0)
            {
                std::vector<NXOpen::TaggedObject*> objects1(2);

                objects1[0] = Feature1;
                objects1[1] = Feature2;

                custom_del(objects1);
 
                edge1_TAG.clear();
                HouDu1 = 100;
            }
            PropertyList* selection0Props = selection0->GetProperties();
            edge1_TAG = selection0Props->GetTaggedObjectVector("SelectedObjects");
            NXOpen::Point3d pickedPoint = selection0Props->GetPoint("PickPoint");
            delete selection0Props;
            selection0Props = NULL;
            if (edge1_TAG.empty())
            {
                UF_terminate();
                return 0;
            }

            NXOpen::Face* selectedFace = dynamic_cast<NXOpen::Face*>(NXOpen::NXObjectManager::Get(edge1_TAG[0]->Tag()));
            if (selectedFace == NULL)
            {
                kakou::theUI->NXMessageBox()->Show("卡口", NXOpen::NXMessageBox::DialogTypeError, "请选择面。");
                UF_terminate();
                return 0;
            }

            vector<Edge*> selectedFaceEdges = selectedFace->GetEdges();
            if (selectedFaceEdges.empty())
            {
                kakou::theUI->NXMessageBox()->Show("卡口", NXOpen::NXMessageBox::DialogTypeError, "所选面没有可用边。");
                UF_terminate();
                return 0;
            }

            NXOpen::Point* pickedPointObject = workPart->Points()->CreatePoint(pickedPoint);
            edge1 = NULL;
            double nearestDistance = 1.0e100;
            for (size_t edgeIndex = 0; edgeIndex < selectedFaceEdges.size(); ++edgeIndex)
            {
                MeasureDistance* pickDistance = workPart->MeasureManager()->NewDistance(NULL, MeasureManager::MeasureTypeMinimum, pickedPointObject, selectedFaceEdges[edgeIndex]);
                double distanceValue = pickDistance->Value();
                if (distanceValue < nearestDistance)
                {
                    nearestDistance = distanceValue;
                    edge1 = selectedFaceEdges[edgeIndex];
                }
            }
            workPart->Points()->DeletePoint(pickedPointObject);
            if (edge1 == NULL)
            {
                kakou::theUI->NXMessageBox()->Show("卡口", NXOpen::NXMessageBox::DialogTypeError, "未找到靠近点击位置的边。");
                UF_terminate();
                return 0;
            }

            NXOpen::Scalar* scalar1;
            scalar1 = workPart->Scalars()->CreateScalar(0.5, NXOpen::Scalar::DimensionalityTypeNone, NXOpen::SmartObject::UpdateOptionWithinModeling);

            NXOpen::Point* point1;
            point1 = workPart->Points()->CreatePoint(edge1, scalar1, NXOpen::SmartObject::UpdateOptionWithinModeling);
            Point3d1=point1->Coordinates();
            manip0->SetOrigin(Point3d1);
            Body1 = edge1->GetBody();
            vector< Edge*>Vedge1= Body1->GetEdges();
            vector< Face*>VFace1 = edge1->GetFaces();

            std::vector<double> area1;

            BodyCollection* bodycollector = workPart->Bodies();
            NXOpen::BodyCollection::iterator Ite = bodycollector->begin();
            Body* WorkBody;

            NXOpen::Unit* areaUnit(dynamic_cast<NXOpen::Unit*>(workPart->UnitCollection()->FindObject("SquareMilliMeter")));
            NXOpen::Unit* lengthUnit(dynamic_cast<NXOpen::Unit*>(workPart->UnitCollection()->FindObject("MilliMeter")));
            std::vector<IParameterizedSurface*> selectedFaceObjects(1);
            selectedFaceObjects[0] = selectedFace;
            MeasureFaces* selectedFaceMeasure;
            selectedFaceMeasure = workPart->MeasureManager()->NewFaceProperties(areaUnit, lengthUnit, 0.2, selectedFaceObjects);
            double selectedFaceArea = selectedFaceMeasure->Area();

            NXOpen::Direction* selectedFaceDirection;
            selectedFaceDirection = workPart->Directions()->CreateDirection(selectedFace, NXOpen::SenseForward, NXOpen::SmartObject::UpdateOptionWithinModeling);
            NXOpen::Vector3d selectedFaceVector = selectedFaceDirection->Vector();
            double selectedFaceVectorLength = std::sqrt(selectedFaceVector.X * selectedFaceVector.X + selectedFaceVector.Y * selectedFaceVector.Y + selectedFaceVector.Z * selectedFaceVector.Z);
            NXOpen::Face* thicknessFace = NULL;

            vector<Face*> bodyFaces = Body1->GetFaces();
            for (size_t i = 0; i < bodyFaces.size(); i++)
            {
                if (bodyFaces[i]->Tag() == selectedFace->Tag())
                {
                    continue;
                }

                try
                {
                    std::vector<IParameterizedSurface*> candidateFaceObjects(1);
                    candidateFaceObjects[0] = bodyFaces[i];
                    MeasureFaces* candidateFaceMeasure;
                    candidateFaceMeasure = workPart->MeasureManager()->NewFaceProperties(areaUnit, lengthUnit, 0.2, candidateFaceObjects);
                    if (candidateFaceMeasure->Area() <= selectedFaceArea * 0.5)
                    {
                        continue;
                    }

                    NXOpen::Direction* candidateFaceDirection;
                    candidateFaceDirection = workPart->Directions()->CreateDirection(bodyFaces[i], NXOpen::SenseForward, NXOpen::SmartObject::UpdateOptionWithinModeling);
                    NXOpen::Vector3d candidateFaceVector = candidateFaceDirection->Vector();
                    double candidateFaceVectorLength = std::sqrt(candidateFaceVector.X * candidateFaceVector.X + candidateFaceVector.Y * candidateFaceVector.Y + candidateFaceVector.Z * candidateFaceVector.Z);
                    if (selectedFaceVectorLength <= 1.0e-6 || candidateFaceVectorLength <= 1.0e-6)
                    {
                        continue;
                    }

                    double dotValue =
                        (selectedFaceVector.X * candidateFaceVector.X +
                            selectedFaceVector.Y * candidateFaceVector.Y +
                            selectedFaceVector.Z * candidateFaceVector.Z) /
                        (selectedFaceVectorLength * candidateFaceVectorLength);
                    if (std::fabs(dotValue) < 0.999)
                    {
                        continue;
                    }

                    MeasureDistance* measureDistance1;
                    measureDistance1 = workPart->MeasureManager()->NewDistance(NULL, MeasureManager::MeasureTypeMinimum, selectedFace, bodyFaces[i]);
                    double distance1 = measureDistance1->Value();
                    if (distance1 < HouDu1 && distance1 > 0)
                    {
                        HouDu1 = distance1;
                        thicknessFace = bodyFaces[i];
                    }
                }
                catch (...)
                {
                    continue;
                }
            }

            if (thicknessFace == NULL)
            {
                workPart->Points()->DeletePoint(point1);
                kakou::theUI->NXMessageBox()->Show("卡口", NXOpen::NXMessageBox::DialogTypeError, "未找到与所选面平行且面积大于所选面50%的面，无法识别厚度。");
                UF_terminate();
                return 0;
            }

            workPart->Points()->DeletePoint(point1);
            for (size_t c = 0; c < VFace1.size(); c++)
            {
                NXOpen::Unit* unit1(dynamic_cast<NXOpen::Unit*>(workPart->UnitCollection()->FindObject("SquareMilliMeter")));
                NXOpen::Unit* unit2(dynamic_cast<NXOpen::Unit*>(workPart->UnitCollection()->FindObject("MilliMeter")));
                std::vector<IParameterizedSurface*> objects1(1);
                objects1[0] = VFace1[c];
                MeasureFaces* measureFaces1;
                measureFaces1 = workPart->MeasureManager()->NewFaceProperties(unit1, unit2, 0.2, objects1);
                area1.push_back(measureFaces1->Area());
            }

            if (area1[0]> area1[1])
            {
                NXOpen::Direction* direction1;
                direction1 = workPart->Directions()->CreateDirection(VFace1[1], NXOpen::SenseForward, NXOpen::SmartObject::UpdateOptionWithinModeling);
                manip0->SetYAxis(direction1->Vector());
                direction1 = workPart->Directions()->CreateDirection(VFace1[0], NXOpen::SenseForward, NXOpen::SmartObject::UpdateOptionWithinModeling);
                manip0->SetXAxis(direction1->Vector());

                for (; Ite != bodycollector->end(); ++Ite)
                {
                    WorkBody = (*Ite);
                    MeasureDistance* measureDistance2;
                    measureDistance2 = workPart->MeasureManager()->NewDistance(NULL, MeasureManager::MeasureTypeMinimum, thicknessFace, WorkBody);
                    double distance2 = measureDistance2->Value();//获取测量的值
                    if (distance2 < 0.5 && WorkBody->Tag() != Body1->Tag())
                    {
                        Bodytool=WorkBody;
                    }
                }
            }
            else
            {
                NXOpen::Direction* direction1;
                direction1 = workPart->Directions()->CreateDirection(VFace1[0], NXOpen::SenseForward, NXOpen::SmartObject::UpdateOptionWithinModeling);
                manip0->SetYAxis(direction1->Vector());
                direction1 = workPart->Directions()->CreateDirection(VFace1[1], NXOpen::SenseForward, NXOpen::SmartObject::UpdateOptionWithinModeling);
                manip0->SetXAxis(direction1->Vector());

                for (; Ite != bodycollector->end(); ++Ite)
                {
                    WorkBody = (*Ite);
                    MeasureDistance* measureDistance2;
                    measureDistance2 = workPart->MeasureManager()->NewDistance(NULL, MeasureManager::MeasureTypeMinimum, thicknessFace, WorkBody);
                    double distance2 = measureDistance2->Value();//获取测量的值
                    if (distance2 < 0.5&& WorkBody->Tag()!= Body1->Tag())
                    {
                        Bodytool = WorkBody;
                    }

                }

            }

            custom_manip_getMatrix(manip0, matrix1);//输出Matrix3x3    
            double ufPoint3d1[3];
            ufPoint3d1[0] = Point3d1.X; ufPoint3d1[1] = Point3d1.Y; ufPoint3d1[2] = Point3d1.Z;

            double newx[3];
            double newy[3];
            X_vec[0] = manip0->XAxis().X; X_vec[1] = manip0->XAxis().Y; X_vec[2] = manip0->XAxis().Z;
            Y_vec[0] = manip0->YAxis().X; Y_vec[1] = manip0->YAxis().Y; Y_vec[2] = manip0->YAxis().Z;

            UF_VEC3_affine_comb(ufPoint3d1, -HouDu1/2, X_vec, newx);
            UF_VEC3_affine_comb(newx, Thedim01/2, Y_vec, newy);
            sprintf(Z, "%.2f", HouDu1);
            sprintf(Za, "%.2f", HouDu1 + Thedim02 * 2);
            if (enum0->GetProperties()->GetEnum("Value")==0)
            {
                point_Zon = workPart->Points()->CreatePoint({ newy[0] ,newy[1] ,newy[2] });

                manip0->SetOrigin({ newy[0] ,newy[1] ,newy[2] });

                custom_box(manip0->Origin(), matrix1, Z, Thardim01, Thardim0, Feature1);
                custom_box(manip0->Origin(), matrix1, Za, Thardim01a, Thardim0a, Feature2);
                UF_OBJ_set_blank_status(Feature2->GetBodies()[0]->Tag(), UF_OBJ_BLANKED);
                vFeature1.push_back(Feature1);
                vFeature1.push_back(Feature2);
            }
            else
            {
                if (enum0->GetProperties()->GetEnum("Value") == 1)
                {
                    Z_vec[0] = matrix1.Zx; Z_vec[1] = matrix1.Zy; Z_vec[2] = matrix1.Zz;

                    PropertyList* dim0Props1 = linear_dim0->GetProperties();
                    double Thedim0 = dim0Props1->GetDouble("Value");
                    delete dim0Props1;
                    dim0Props1 = NULL;

                    PropertyList* dim03Props1 = linear_dim03->GetProperties();
                    double Thedim03 = dim03Props1->GetDouble("Value");
                    delete dim03Props1;
                    dim03Props1 = NULL;

                    double Length = (edge1->GetLength()- Thedim0- Thedim03*2)/2;
                    double point3[3];
                    UF_VEC3_affine_comb(newy, Length, Z_vec, point3);
                    manip0->SetOrigin({ point3[0] ,point3[1] ,point3[2] });
                    custom_box(manip0->Origin(), matrix1, Z, Thardim01, Thardim0, Feature1);
                    custom_box(manip0->Origin(), matrix1, Za, Thardim01a, Thardim0a, Feature2);
                    UF_VEC3_affine_comb(newy, -Length, Z_vec, point3);
                    manip0->SetOrigin({ point3[0] ,point3[1] ,point3[2] });
                    custom_box(manip0->Origin(), matrix1, Z, Thardim01, Thardim0, Feature3);
                    custom_box(manip0->Origin(), matrix1, Za, Thardim01a, Thardim0a, Feature4);
                    UF_OBJ_set_blank_status(Feature2->GetBodies()[0]->Tag(), UF_OBJ_BLANKED);
                    UF_OBJ_set_blank_status(Feature4->GetBodies()[0]->Tag(), UF_OBJ_BLANKED);
                    vFeature1.push_back(Feature1);
                    vFeature1.push_back(Feature2);
                    vFeature1.push_back(Feature3);
                    vFeature1.push_back(Feature4);
                }
                else
                {
                    if (enum0->GetProperties()->GetEnum("Value") == 2)
                    {
                        manip0->SetOrigin({ newy[0] ,newy[1] ,newy[2] });

                        custom_box(manip0->Origin(), matrix1, Z, Thardim01, Thardim0, Feature1);
                        custom_box(manip0->Origin(), matrix1, Za, Thardim01a, Thardim0a, Feature2);

                        Z_vec[0] = matrix1.Zx; Z_vec[1] = matrix1.Zy; Z_vec[2] = matrix1.Zz;

                        PropertyList* dim0Props1 = linear_dim0->GetProperties();
                        double Thedim0 = dim0Props1->GetDouble("Value");
                        delete dim0Props1;
                        dim0Props1 = NULL;

                        PropertyList* dim03Props1 = linear_dim03->GetProperties();
                        double Thedim03 = dim03Props1->GetDouble("Value");
                        delete dim03Props1;
                        dim03Props1 = NULL;

                        double Length = (edge1->GetLength() - Thedim0 - Thedim03 * 2) / 2;
                        double point3[3];
                        UF_VEC3_affine_comb(newy, Length, Z_vec, point3);
                        manip0->SetOrigin({ point3[0] ,point3[1] ,point3[2] });
                        custom_box(manip0->Origin(), matrix1, Z, Thardim01, Thardim0, Feature3);
                        custom_box(manip0->Origin(), matrix1, Za, Thardim01a, Thardim0a, Feature4);
                        UF_VEC3_affine_comb(newy, -Length, Z_vec, point3);
                        manip0->SetOrigin({ point3[0] ,point3[1] ,point3[2] });
                        custom_box(manip0->Origin(), matrix1, Z, Thardim01, Thardim0, Feature5);
                        custom_box(manip0->Origin(), matrix1, Za, Thardim01a, Thardim0a, Feature6);
                        UF_OBJ_set_blank_status(Feature2->GetBodies()[0]->Tag(), UF_OBJ_BLANKED);
                        UF_OBJ_set_blank_status(Feature4->GetBodies()[0]->Tag(), UF_OBJ_BLANKED);
                        UF_OBJ_set_blank_status(Feature6->GetBodies()[0]->Tag(), UF_OBJ_BLANKED);
                        vFeature1.push_back(Feature1);
                        vFeature1.push_back(Feature2);
                        vFeature1.push_back(Feature3);
                        vFeature1.push_back(Feature4);
                        vFeature1.push_back(Feature5);
                        vFeature1.push_back(Feature6);
                    }
                    else
                    {
                        if (enum0->GetProperties()->GetEnum("Value") == 3)
                        {
                            Z_vec[0] = matrix1.Zx; Z_vec[1] = matrix1.Zy; Z_vec[2] = matrix1.Zz;

                            PropertyList* dim0Props1 = linear_dim0->GetProperties();
                            double Thedim0 = dim0Props1->GetDouble("Value");
                            delete dim0Props1;
                            dim0Props1 = NULL;

                            PropertyList* dim03Props1 = linear_dim03->GetProperties();
                            double Thedim03 = dim03Props1->GetDouble("Value");
                            delete dim03Props1;
                            dim03Props1 = NULL;

                            double Length = (edge1->GetLength() - Thedim0 - Thedim03 * 2) / 3;
                            double point3[3];
                            UF_VEC3_affine_comb(newy, Length/2, Z_vec, point3);
                            manip0->SetOrigin({ point3[0] ,point3[1] ,point3[2] });
                            custom_box(manip0->Origin(), matrix1, Z, Thardim01, Thardim0, Feature1);
                            custom_box(manip0->Origin(), matrix1, Za, Thardim01a, Thardim0a, Feature2);
                            UF_VEC3_affine_comb(newy, -Length/2, Z_vec, point3);
                            manip0->SetOrigin({ point3[0] ,point3[1] ,point3[2] });
                            custom_box(manip0->Origin(), matrix1, Z, Thardim01, Thardim0, Feature3);
                            custom_box(manip0->Origin(), matrix1, Za, Thardim01a, Thardim0a, Feature4);
                            UF_VEC3_affine_comb(newy, Length / 2+ Length, Z_vec, point3);
                            manip0->SetOrigin({ point3[0] ,point3[1] ,point3[2] });
                            custom_box(manip0->Origin(), matrix1, Z, Thardim01, Thardim0, Feature5);
                            custom_box(manip0->Origin(), matrix1, Za, Thardim01a, Thardim0a, Feature6);
                            UF_VEC3_affine_comb(newy, -(Length / 2 + Length), Z_vec, point3);
                            manip0->SetOrigin({ point3[0] ,point3[1] ,point3[2] });
                            custom_box(manip0->Origin(), matrix1, Z, Thardim01, Thardim0, Feature7);
                            custom_box(manip0->Origin(), matrix1, Za, Thardim01a, Thardim0a, Feature8);
                            UF_OBJ_set_blank_status(Feature2->GetBodies()[0]->Tag(), UF_OBJ_BLANKED);
                            UF_OBJ_set_blank_status(Feature4->GetBodies()[0]->Tag(), UF_OBJ_BLANKED);
                            UF_OBJ_set_blank_status(Feature6->GetBodies()[0]->Tag(), UF_OBJ_BLANKED);
                            UF_OBJ_set_blank_status(Feature8->GetBodies()[0]->Tag(), UF_OBJ_BLANKED);
                            vFeature1.push_back(Feature1);
                            vFeature1.push_back(Feature2);
                            vFeature1.push_back(Feature3);
                            vFeature1.push_back(Feature4);
                            vFeature1.push_back(Feature5);
                            vFeature1.push_back(Feature6);
                            vFeature1.push_back(Feature7);
                            vFeature1.push_back(Feature8);
                        }
                    }
                }
            }
            UF_terminate();
        }
        else if (block == manip0)
        {
            custom_manip_getMatrix(manip0,matrix1);//输出Matrix3x3     
            custom_rebox(manip0->Origin(), matrix1, Z, Thardim01, Thardim0, Feature1);
            custom_rebox(manip0->Origin(), matrix1, Za, Thardim01a, Thardim0a, Feature2);

        }
    }
    catch(exception& ex)
    {
        //---- Enter your exception handling code here -----
        kakou::theUI->NXMessageBox()->Show("Block Styler", NXOpen::NXMessageBox::DialogTypeError, ex.what());
    }
    return 0;
}

//------------------------------------------------------------------------------
//Callback Name: ok_cb
//------------------------------------------------------------------------------
int kakou::ok_cb()
{
    int errorCode = 0;
    try
    {
        errorCode = apply_cb();
    }
    catch(exception& ex)
    {
        //---- Enter your exception handling code here -----
        errorCode = 1;
        kakou::theUI->NXMessageBox()->Show("Block Styler", NXOpen::NXMessageBox::DialogTypeError, ex.what());
    }
    return errorCode;
}

//------------------------------------------------------------------------------
//Function Name: GetBlockProperties
//Description: Returns the propertylist of the specified BlockID
//------------------------------------------------------------------------------
PropertyList* kakou::GetBlockProperties(const char *blockID)
{
    return theDialog->GetBlockProperties(blockID);
}


